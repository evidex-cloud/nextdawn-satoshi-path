export default {
  id: "script",
  stage: 4,
  order: 2,
  title: "Bitcoin Script：每个输出都是一道锁",
  difficulty: "core",
  prereqs: ["utxo", "signatures"],

  oneLiner:
    "每个 UTXO 都背着一段“锁定脚本”——一道用比特币自己的小型脚本语言写成的锁。想花掉它，你得在输入里给出一段“解锁脚本”（通常是签名）当钥匙；节点把钥匙和锁拼起来、用一台栈式小机器跑一遍，结果为真才放行。",

  intuition: `
上一节说每个 UTXO 都带一把**锁**。这把锁，不是一个静态密码，而是一小段**程序**——用比特币内置的脚本语言 **Bitcoin Script** 写成。"谁能花掉这枚币"这件事，本质上是"谁能让这段程序跑出'真'"。

它的运作方式出奇地朴素：每个输出里有一段**锁定脚本（\`scriptPubKey\`）**，规定花费条件；你想花它时，在输入里提供一段**解锁脚本（\`scriptSig\`）**当钥匙（通常是\`签名 + 公钥\`）。节点把**钥匙在前、锁在后**拼成一整段，丢进一台**栈式小机器**从左到右跑一遍——跑完栈顶为"真"，锁就开，这笔花费有效。

最常见的锁叫 **P2PKH**，翻译成大白话："谁能拿出与这个地址匹配的公钥、并用对应私钥签个名，谁就能花。"——这正好把阶段 3 的数字签名用上了。

右边是一台**真的**脚本栈机：给你一道"凑出和为 15"的数字锁，你填钥匙、单步执行，看每个操作码怎么改写栈、最后锁怎么开。

**这一节，我们把 Bitcoin Script 拆成四块：**

- **① 栈式机器怎么跑——压栈、操作码、栈顶为真**
- **② 走通一个 P2PKH——逐操作码看锁怎么被打开**
- **③ 为什么故意"不图灵完备"——安全设计的取舍**
- **④ 从 P2PKH 到 P2SH / 隔离见证——锁的家族树**
`,

  mechanics: `
### ① 栈式机器：压栈、操作码、栈顶为真

Bitcoin Script 没有变量、没有内存地址，只有一个**栈**（后进先出的一叠盘子）和从左到右的一串"词"。词只有两类：

- **数据**（数字、签名、公钥）：遇到就**压入栈顶**。
- **操作码（\`OP_\` 开头）**：从栈顶**弹出**若干个数据、做运算、再把结果压回。比如 \`OP_ADD\` 弹出两个数、相加、压回它们的和；\`OP_EQUAL\` 弹出两个数、相等就压回 \`1\`（真）否则 \`0\`（假）。

**验证 = 拼接执行。** 节点把解锁脚本和锁定脚本接成一整段跑完，看栈顶：非零且非空即"真"，这笔花费通过。整个过程没有随机、没有外部输入，**任何节点跑同一段脚本结果必然一样**——这是全网能就"这笔花费是否有效"达成一致的前提。

> 历史坑：早期 \`scriptSig\` 和 \`scriptPubKey\` 是真的字符串拼接后一起跑。后来为堵住安全漏洞，改成**两段分开跑、用栈状态传递**（先跑解锁脚本，把结果栈交给锁定脚本接着跑），但你仍可以按"钥匙在前、锁在后"来理解。

### ② 走通一个 P2PKH：逐操作码看锁怎么被打开

经典的 P2PKH 锁定脚本是这 5 个词：

\`OP_DUP  OP_HASH160  <20 字节公钥哈希>  OP_EQUALVERIFY  OP_CHECKSIG\`

解锁脚本（钥匙）提供两样：\`<签名> <公钥>\`。拼起来执行，栈的变化是这样（自上而下读栈顶）：

- 压入 \`<签名>\`、再压入 \`<公钥>\` → 栈：\`公钥, 签名\`。
- \`OP_DUP\`：复制栈顶的公钥 → 栈：\`公钥, 公钥, 签名\`。
- \`OP_HASH160\`：把栈顶公钥做一次 SHA-256 再一次 RIPEMD-160，得到 20 字节哈希 → 栈：\`公钥哈希', 公钥, 签名\`。
- 压入锁里写死的 \`<公钥哈希>\` → 栈：\`公钥哈希, 公钥哈希', 公钥, 签名\`。
- \`OP_EQUALVERIFY\`：弹出两个哈希比对，**不相等就立刻终止判失败**；相等则继续 → 栈：\`公钥, 签名\`。
- \`OP_CHECKSIG\`：弹出公钥和签名，验证这个签名确实是该公钥对应私钥对**本交易**签的；通过则压入 \`1\`。

栈顶剩一个 \`1\`，锁打开。注意这里检验了两件独立的事：**你给的公钥确实哈希成地址里那个值**（证明地址是你的），**而且你拿对应私钥签了名**（证明你此刻同意花）。地址（那 20 字节哈希）正是这把锁向外公开的"门牌"。

### ③ 为什么故意"不图灵完备"

Bitcoin Script **没有循环、没有跳转、不能递归**——它故意不是图灵完备的。这不是能力不足，是**安全设计**：

- 全网**每一个节点**都要重新执行每一笔交易的脚本来验证它。如果脚本能写无限循环，一个恶意输出就能让所有验证它的节点卡死——这正是后来以太坊要用"gas 收费"才能驯服的问题。
- 比特币换了个更简单的思路：**结构上就不可能跑太久**。没有循环，脚本的执行步数有上限，验证成本**可预测、有界**。再加上栈深、操作码数量等硬性限制，单笔脚本绝不会拖垮网络。
- 代价是表达力受限：你能写多签、时间锁、哈希锁这类"条件式"花费，但写不了任意复杂的链上计算。比特币选择把可编程性留得克制——**可验证性优先于可编程性**，这是它和"世界计算机"路线的根本分野。

### ④ 从 P2PKH 到 P2SH 与隔离见证：锁的家族树

P2PKH 只是最老的一种锁。同一台栈机，配不同的脚本模板，就长出一整个家族（下一节按地址类型细看）：

- **P2SH（\`3\` 开头）**：锁里**只放一段脚本的哈希**（\`OP_HASH160 <脚本哈希> OP_EQUAL\`）。花费时，你先亮出**完整的赎回脚本**、证明它哈希等于锁里那个值，再满足这段脚本本身。好处：复杂条件（如\`2-of-3\` 多签）的负担从**收款人**转到**花费人**——收款方只需给你一个短地址，不必把那一大段多签脚本塞进自己的输出。
- **P2WPKH / P2WSH（隔离见证，\`bc1q\` 开头）**：把"钥匙"（签名、公钥）从 \`scriptSig\` 挪到交易专设的**见证区**。锁定脚本退化成极简的 \`OP_0 <哈希>\`，真正的验证逻辑由见证数据驱动。这样签名不再计入 \`txid\`（根治交易延展性），见证数据还享**费用折扣**（阶段 4.4）。
- **P2TR（Taproot，\`bc1p\` 开头）**：基于 Schnorr 签名，能把"一棵脚本树"藏在一个公钥背后——只走常规路径时，链上看到的和一笔普通转账毫无二致，**复杂即隐形**（阶段 8 深入）。

记住一句话：**地址只是某段锁定脚本（或其哈希、公钥）的人类可读编码**。换地址类型，换的是锁的样式和摆放方式，背后跑的始终是同一台栈式小机器。
`,

  demo: "script-machine",

  analogy: `
把每个 UTXO 想成一个**带条件锁的保险箱**，箱子表面刻着开锁条件（锁定脚本）。

你要取钱，就得递上**钥匙串**（解锁脚本：签名和公钥）。验钞员（节点）把你的钥匙串和箱子上的条件**接在一起、照章一步步比对**：钥匙对、签名真、最后亮起绿灯（栈顶为真），箱子才打开。任何一步不符，箱子纹丝不动。
`,

  misconceptions: [
    "“比特币只能简单转账，没有可编程性。” —— 它有脚本语言，能表达多签、时间锁、哈希锁等条件；只是为了安全，故意不做成图灵完备。",
    "“锁定脚本把钱‘锁在地址里’。” —— 锁定脚本是一段花费条件；满足条件的人就能花。地址只是某类锁定脚本的简短编码。",
    "“解锁脚本是在‘解密’。” —— 不是。它只提供能让整段脚本跑出‘真’的数据（通常是签名），节点做的是‘执行并验证’而非‘解密’。",
    "“脚本能无限循环、做任意计算。” —— 不能。Bitcoin Script 没有循环、非图灵完备，执行步数有上限，确保每个节点都能快速、有界地验证。",
    "“P2PKH 里只验证了签名。” —— 验证了两件事：你给的公钥确实哈希成锁里那个地址，且你用对应私钥对本交易签了名。前者证明地址是你的，后者证明你同意花。",
  ],

  quiz: [
    {
      q: "每个 UTXO 上的“锁”叫什么？",
      options: ["解锁脚本", "锁定脚本（scriptPubKey）", "私钥", "默克尔根"],
      answer: 1,
      explain: "输出带锁定脚本，规定满足什么条件才能花。",
    },
    {
      q: "花一个 UTXO 时，你在输入里提供的“钥匙”通常是？",
      options: ["你的私钥本体", "签名加公钥（解锁脚本）", "你的余额", "一笔手续费"],
      answer: 1,
      explain: "解锁脚本通常是签名加公钥，私钥本身永不出场。",
    },
    {
      q: "节点怎么判断一笔花费是否有效？",
      options: ["问交易所", "把解锁脚本与锁定脚本拼起来执行，栈顶为真即通过", "看金额大小", "随机抽查"],
      answer: 1,
      explain: "栈式机器跑完，结果为真则解锁。",
    },
    {
      q: "Bitcoin Script 为什么故意不做成图灵完备？",
      options: ["开发者偷懒", "为安全：避免无限循环或复杂脚本拖垮每个验证节点", "为了省电", "因为没人会用"],
      answer: 1,
      explain: "有界、可快速验证，是去中心化网络的安全前提。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Script（图文）", url: "https://learnmeabitcoin.com/technical/script/" },
    { label: "learnmeabitcoin：P2PKH", url: "https://learnmeabitcoin.com/technical/script/p2pkh/" },
  ],
};
