export default {
  id: "segwit",
  stage: 8,
  order: 2,
  title: "SegWit 隔离见证：把签名搬出去",
  difficulty: "core",
  prereqs: ["script", "tx-size-fees", "forks-bips"],

  oneLiner:
    "SegWit 把交易里的“见证”（签名）从主体数据中分离出来。这一挪，顺手解决了三件事：根治交易延展性（为闪电网络铺路）、给见证数据打折（更便宜）、并通过新的“重量”计量方式温和地提升了区块容量。",

  intuition: `
"隔离见证（Segregated Witness，SegWit）"这个名字其实很直白：把交易里的**见证**（也就是**签名**）**隔离出去**，单独放一块区域。就这么一挪，连解三个老问题：

- **① 根治交易延展性。** 老办法里签名是交易主体的一部分，会被算进 **txid**。可签名的编码能被第三方"描摹"微调（不改金额却改了 txid）。任何引用了某笔**未确认交易**的协议（比如闪电网络的通道）都会因此崩掉。SegWit 把签名移出 txid 的计算 → **txid 稳定**，二层才能安全建立。
- **② 见证打折、更便宜。** 见证数据在计费时只按四分之一计权（阶段 4.4 的 vByte 折扣就来自这里）。
- **③ 温和扩容。** 用新的"重量"上限取代了死板的 1MB，让区块能装下更多（尤其是签名密集的）交易。

更妙的是：这三件事其实是**同一个动作**（把见证挪出去）的三个副产品——这正是 SegWit 设计的精巧之处。而它还得在**不分裂网络**的前提下完成（区块大小之争的余波，回扣 8.1），于是工程师们想出了一个绝妙的"向后兼容"招法。

右边的演示：让第三方"描摹"一下签名，对比传统交易和 SegWit 交易的 txid 谁变谁不变。

**这一节，我们把 SegWit 拆成五块：**

- **① 交易延展性到底是什么——一个困扰多年的老 bug**
- **② "隔离见证"具体怎么挪——txid 与 wtxid 的分家**
- **③ 重量与容量——4 倍折扣是怎么算出来的**
- **④ 它凭什么是软分叉——"任何人可花"的障眼法**
- **⑤ 连带的收获——bech32 地址与为 Taproot 铺路**
`,

  mechanics: `
### ① 交易延展性：一个老 bug

**延展性（malleability）**指的是：第三方能在**不让签名失效、不改金额、不偷到钱**的前提下，改变交易的字节表示，从而改变它的 **txid**。

- 怎么做到的？早期比特币的签名编码（DER 格式 + 脚本里的一些操作）存在多种"等价写法"。比如 ECDSA 签名 \`(r, s)\` 和 \`(r, -s mod n)\` 在数学上都有效，但字节不同 → 哈希出的 txid 就不同。一个中间人甚至矿工，可以把你广播的交易**改头换面**后再打包。
- 对**普通转账**这没大碍：钱照样到账，只是 txid 变了，你按原 txid 查不到而已。
- 但对**二层协议是致命的**。闪电通道的玩法是："我先签好一笔花费**未确认资金交易**的承诺交易，把它攥在手里当保险"（下一节）。可如果那笔资金交易的 txid 能被人改掉，我手里这张"保险"引用的就是个**不存在的 txid**，瞬间作废——通道资金就可能被对方卷走。**延展性不修，闪电网络根本无法安全运行。**

### ② "隔离"具体怎么挪：txid 与 wtxid 分家

SegWit 的核心手术：**把见证（签名 + 解锁脚本）从交易主体里搬出去，单独放进一个"见证区"，并让 txid 的计算不再包含它。**

- 于是一笔交易现在有**两个哈希**：**txid**（只哈希"非见证"部分，金额、输入引用、输出等）和 **wtxid**（包含见证的完整哈希）。
- 既然 txid 不再碰签名，**别人再怎么"描摹"签名，txid 都纹丝不动** → 延展性根治。
- 见证数据则通过区块里一个新增的**见证默克尔根**（藏在 coinbase 交易里）来承诺，保证它没被篡改。老节点看不到这个结构，但新节点会校验。

### ③ 重量与容量：4 倍折扣怎么算

SegWit 顺势重新定义了区块容量。区块上限从"1MB 字节"改成"**400 万重量单位（weight units, WU）**"：

- **基础数据**（非见证部分）每字节计 **4** WU；**见证数据**每字节计 **1** WU。
- 于是 **vByte = 重量 ÷ 4**（回扣 4.4 的费率计量）。一笔交易的签名越多（如多签、复杂脚本），落在"打 1 折"的见证区里的字节就越多，等效体积越小、越省费。
- **为什么是这么个折扣？** 它是一个精心调过的旋钮：既给见证数据让利（鼓励大家用 SegWit），又把**最大区块控制在约 4MB 的理论上限、实际约 1.5–2MB**——这样既温和扩了容，又**没有真把节点负担推到大区块派想要的程度**，守住了去中心化底线（回扣 8.1）。一个数字，同时安抚了两种诉求。

### ④ 它凭什么是软分叉：向后兼容的障眼法

最难的部分：怎样在**不强迫所有人升级、不分裂网络**的前提下加这套东西（回扣 6.4）？

- 答案是利用一条老规则。SegWit 的输出在**老节点眼里**被构造成 \`OP_TRUE\` 之类"**任何人都能花（anyone-can-spend）**"的脚本——按老规则，这完全合法，老节点照单全收。
- 但**升级后的新节点**看到的是另一层规则：要花这个输出，必须在见证区提供有效签名。新节点强制执行这条更严的规则。
- 结果：老节点觉得"一切正常"继续验证、继续接受这些区块，新节点则真正执行 SegWit 规则——**网络不分裂**。这是软分叉"用更严的规则收紧、而非放宽"的经典手法。（代价是老节点其实没在校验那部分签名，所以需要足够多的算力和经济多数升级来保证安全。）

### ⑤ 连带的收获：bech32 与为 Taproot 铺路

SegWit 还顺手带来了两样长远的东西：

- **全新的 bech32 地址（bc1q 开头）**：纠错能力更强（打错一两个字符能被检测出来）、全小写更易读、二维码更小。这就是你今天看到的主流地址格式。
- **版本化的升级空间**：SegWit 引入了"见证版本"的概念（v0 就是现在的 bech32）。这给后来的 **Taproot（见证 v1，bc1p 开头）** 预留了**干净的升级通道**——不必再动一次大手术（本阶段最后一节）。可以说 SegWit 不只解决了眼前问题，还为比特币未来几年的演进**修好了路基**。
`,

  demo: "malleability",

  analogy: `
旧交易像一份**正文和签名写在同一张纸、连签名一起复印进编号**的合同——别人把你的签名笔迹描一描，合同编号就变了，凡是按旧编号找这份合同的人全都扑空。

SegWit 把签名**撕到附页**：正文编号（txid）从此只认正文，签名再怎么被人描摹也不影响编号。既稳定了编号（救活了闪电网络），又因为附页"算钱打折"而更便宜。
`,

  misconceptions: [
    "“SegWit 只是为了让交易更便宜。” —— 便宜是红利，更深层是根治交易延展性，从而让闪电网络等二层成为可能。",
    "“SegWit 是一次硬分叉。” —— 不是。它是向后兼容的软分叉，靠‘任何人可花’的障眼法让老节点照样接受新区块，没有分裂。",
    "“见证数据不算进区块大小。” —— 算的，只是按四分之一计权（4 WU vs 1 WU，vByte 折扣的来源）。",
    "“延展性能让别人偷走我转账的钱。” —— 不能。它只改变 txid 的字节表示，不改金额、不改收款方；真正受害的是引用未确认交易的二层协议。",
    "“只有用 SegWit 地址的人才受影响。” —— 是发送方交易的结构在变；用 bc1 地址能让交易 vByte 更小，对省费实实在在。",
  ],

  quiz: [
    {
      q: "SegWit 把交易里的什么“隔离”了出去？",
      options: ["金额", "见证（签名）", "收款地址", "手续费"],
      answer: 1,
      explain: "把签名移到独立的见证区域。",
    },
    {
      q: "SegWit 最深远的贡献是？",
      options: ["让地址更短", "根治交易延展性，使 txid 稳定，为闪电网络等二层铺路", "提高总量上限", "取消手续费"],
      answer: 1,
      explain: "延展性修复是关键，便宜与扩容是红利。",
    },
    {
      q: "“交易延展性”指的是？",
      options: ["交易金额可变", "第三方能在不让签名失效的前提下改变 txid", "区块大小可变", "手续费可变"],
      answer: 1,
      explain: "txid 被第三方改动，会击垮引用未确认交易的协议。",
    },
    {
      q: "SegWit 是哪种升级？",
      options: ["硬分叉，会分裂", "向后兼容的软分叉，不分裂", "中心化升级", "私有改动"],
      answer: 1,
      explain: "老节点仍接受新区块，平滑激活（回扣 6.4）。",
    },
  ],

  further: [
    { label: "维基百科：SegWit", url: "https://en.wikipedia.org/wiki/SegWit" },
    { label: "learnmeabitcoin：Segregated Witness", url: "https://learnmeabitcoin.com/technical/transaction/segwit/" },
  ],
};
