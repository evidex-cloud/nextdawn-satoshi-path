export default {
  id: "p2p-network",
  stage: 6,
  order: 1,
  title: "P2P 网络：没有服务器的“八卦网”",
  difficulty: "core",
  prereqs: ["consensus"],

  oneLiner:
    "比特币没有中心服务器。成千上万个节点彼此直连成一张点对点网络，新交易和新区块像八卦一样在节点间一传十、十传百地扩散；任何人都能加入、谁也不是中心。",

  intuition: `
你用钱包发一笔交易，它并不是发给"比特币公司的服务器"——根本没有这种东西。比特币跑在一张 **P2P（点对点）网络**上：每个**节点**都既是客户端、又是服务器，彼此直连，谁也不比谁更"中心"。

新交易、新区块，就从一个节点**转发给它的邻居**，邻居再转发给各自的邻居……像八卦一样在人群里扩散，几秒内传遍全球。**加入无需许可**：下载软件、连上几个邻居，你就是网络的一份子。正因为没有中心，这张网**极难被关停**——你想封杀它，得同时堵住全世界每一个节点。

右边的演示：从一个节点广播，看消息怎么一跳跳传遍全网。

**这一节，我们把"P2P 网络"拆成四块：**

- **① 节点怎么找到彼此——从 DNS 种子到地址八卦**
- **② 一条消息怎么传遍全网——inv / getdata 的握手**
- **③ 为什么先验证再转发——网络自带的"免疫系统"**
- **④ 为什么这张网关不掉——去中心化在基础设施层的体现**
`,

  mechanics: `
### ① 节点怎么找到彼此

一个新节点开机时，自己谁也不认识，要靠两步"破冰"：

- **DNS 种子（DNS seeds）**：Bitcoin Core 内置了几个由社区维护的域名（如 \`seed.bitcoin.sipa.be\`）。节点解析这些域名，会拿回**一批当前在线节点的 IP**，作为第一批连接对象。这是唯一带点"中心味"的一步，所以特意硬编码了**多个独立运营方**，防止单点作恶。
- **地址八卦（addr gossip）**：连上第一批邻居后，节点之间用 \`addr\` / \`getaddr\` 消息**互相交换"我还认识谁"的地址名单**。靠这种口耳相传，节点很快就攒下一个本地地址库（peers.dat），下次开机直接用，不必再依赖 DNS 种子。

> 还有兜底机制：软件里**硬编码了一小撮"种子节点"地址**，万一所有 DNS 种子都失灵，仍能靠它们冷启动。

### ② 一条消息怎么传遍全网

节点之间不会傻乎乎地把整笔交易见谁发谁，而是走一套**"先吆喝、按需取"**的握手，避免重复传输：

- **inv（inventory，库存通告）**：节点 A 收到新交易后，只向邻居广播一条很小的消息——"我这有个新东西，它的哈希是 xxx"。
- **getdata（索取）**：邻居 B 一看这个哈希**自己还没有**，才回一条 \`getdata\` 去"要"完整数据；如果早就有了，就什么都不做。
- **tx / block（投递）**：A 这才把完整的交易或区块发给 B。

这套机制叫 **gossip（流言传播）**。一笔交易通常**几秒内**就能覆盖全网绝大多数节点。区块传播还有优化：节点常用**紧凑区块（compact blocks，BIP152）**，因为邻居的 mempool 里大概率已经有这些交易了，所以只需传"区块头 + 一串短交易 ID"，对方就能在本地把整块拼出来，省下大量带宽（阶段 6.3 细讲区块传播）。

### ③ 为什么先验证再转发

这是 P2P 网络最关键、却最容易被忽略的一点：**每个节点都先按完整共识规则验证，再决定转不转发**（回扣阶段 2.4）。

- 收到一笔交易，节点会检查：引用的 UTXO 还在不在、签名对不对、有没有双花、费率够不够进自己的 mempool……**一项不过，就直接丢弃，绝不转发**。
- 区块同理：不满足工作量证明、含非法交易的块，会被当场拒收。
- 效果是：一条假消息**最多骚扰它直连的那一圈邻居，就被层层"免疫"挡死**，根本扩散不开。所以恶意节点既骗不了你、也污染不了网络——这就是为什么"自己跑节点"才是真正的去信任。

### ④ 为什么这张网关不掉

把上面三点合起来，就得到比特币基础设施层的抗审查性：

- **没有单点**：2026 年全网约有 **数万个可达全节点**（外加更多藏在防火墙后的不可达节点），分布在上百个国家。没有哪台服务器一关，网络就停。
- **韧性**：个别节点掉线、被封、被攻击，邻居自动重连其它节点，网络照常运转——它的拓扑天生就是为"局部受损仍整体存活"设计的。
- **抗封锁**：真要全球同步封杀，技术上近乎不可能；而且节点还能走 **Tor、I2P** 等隐藏网络（阶段 6.2 会再提到节点的运行形态），进一步抬高封锁门槛。

这正是去中心化从"账本"延伸到"网络基础设施"的体现：不光数据没有中心，**连传播数据的管道也没有中心**。
`,

  demo: "gossip",

  analogy: `
比特币网络就像**小镇里的八卦**：没有广播电台，但每个人都会把听到的新鲜事告诉几个熟人，熟人再告诉各自的熟人——一条消息很快传遍全镇。

想"封杀"一条八卦？你得同时捂住每一个人的嘴。对一张几万节点、遍布全球的网络来说，这几乎办不到。
`,

  misconceptions: [
    "“交易要发给比特币的中央服务器。” —— 没有中央服务器。交易先广播给你节点的几个邻居，再靠 gossip 一跳跳扩散到全网，没有中心受理方。",
    "“关掉几个大节点就能让比特币停摆。” —— 不能。网络由全球数万个对等节点组成，个别掉线时邻居自动重连其它节点，毫无影响。",
    "“加入比特币网络需要审批。” —— 不需要。下载节点软件、靠 DNS 种子连上邻居即可，无需许可、无需注册。",
    "“节点会无条件转发收到的一切。” —— 不会。每个节点先按完整共识规则验证，不合规的直接丢弃、不转发——假消息最多骚扰一圈邻居就被免疫挡死。",
    "“DNS 种子是个中心，封了它比特币就完了。” —— 不会。DNS 种子只用于冷启动那一下；之后节点靠 addr 八卦和本地地址库认识同伴，还有硬编码种子节点兜底。",
  ],

  quiz: [
    {
      q: "比特币交易最终被发往哪里？",
      options: ["比特币官方服务器", "点对点网络里你节点的若干邻居，再层层转发", "某个交易所", "中本聪的电脑"],
      answer: 1,
      explain: "没有中心服务器；靠节点间八卦式转发扩散。",
    },
    {
      q: "新节点靠什么找到第一批邻居？",
      options: ["打客服电话", "内置的 DNS 种子，之后再互相交换地址名单", "扫描全网 IP", "向矿池注册"],
      answer: 1,
      explain: "DNS 种子启动连接，addr 八卦扩展同伴。",
    },
    {
      q: "为什么比特币网络极难被关停？",
      options: ["有军队保护", "去中心化，没有单点可攻击；个别节点掉线网络照常", "节点都在地下", "政府不允许关"],
      answer: 1,
      explain: "封杀要同时堵住全世界每个节点，几乎不可能。",
    },
    {
      q: "节点收到一笔新交易后会？",
      options: ["无条件转发", "先用规则验证，合规才接受并转发给邻居", "先收手续费", "上报中心"],
      answer: 1,
      explain: "先验证再传播，不合规的传不开。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Network / Nodes", url: "https://learnmeabitcoin.com/beginners/guide/" },
    { label: "Bitcoin Core：运行一个全节点", url: "https://bitcoin.org/zh_CN/full-node" },
  ],
};
