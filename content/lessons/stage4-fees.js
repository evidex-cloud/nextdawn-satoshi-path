export default {
  id: "tx-size-fees",
  stage: 4,
  order: 4,
  title: "手续费与交易大小：vByte 到底是什么",
  difficulty: "core",
  prereqs: ["utxo", "address-types"],

  oneLiner:
    "比特币手续费 = 费率 × 交易大小，而大小的单位是“虚拟字节（vByte）”。交易越大越贵，而大小主要由输入和输出的数量、以及地址类型决定——隔离见证的数据享受“折扣”，所以用 SegWit/Taproot 地址、少用零碎 UTXO，都能更省钱。",

  intuition: `
阶段 1 说过：手续费 = **费率（sat/vB）× 交易大小（vB）**，和转账金额无关——转 1 聪还是转 1000 BTC，只要交易"个头"一样大，手续费就一样。这一节把"大小"这件事彻底讲清楚。

决定交易大小的，主要是三件事：**输入的数量**（每多花一个 UTXO 就多一份"钥匙"数据，所以 UTXO 太碎会变贵）、**输出的数量**（通常一个付款 + 一个找零）、以及**地址类型**（隔离见证把签名放进"见证"区，而见证在计费时**打了折**，所以同一笔交易用 \`bc1\` 地址往往比用 \`1\` 地址便宜不少）。

这个"打折后的大小"，用 **虚拟字节（vByte / vB）** 来衡量。一笔典型交易大约一两百 vB。

右边的估算器：调输入/输出数量、切换地址类型，看交易大小和手续费怎么变。

**这一节，我们把"交易大小与手续费"拆成四块：**

- **① 手续费的来源——为什么没有"手续费字段"**
- **② vByte 是怎么算出来的——权重、见证折扣、4 倍计权**
- **③ 拆解一笔交易的 vByte——输入、输出、固定开销的经验数字**
- **④ 省费三招——地址类型、UTXO 管理、费率时机**
`,

  mechanics: `
### ① 手续费的来源：为什么没有"手续费字段"

比特币交易里**根本没有一个写着"手续费"的字段**（回扣阶段 4.1）：

- **手续费 = 输入总额 − 输出总额**，这个差额由打包它的矿工在 coinbase 里收走（阶段 5.5）。你是靠"故意少写一点输出"来付费的。
- 所以"我愿意付多少费"不是直接填个数，而是**钱包替你算**：先估出这笔交易的 vByte，乘上你选的费率，得到该留多少差额，再把找零输出相应调小。
- ⚠ 这也是为什么**漏写找零是灾难**：多出来的全部变成手续费。历史上真有人因此付出数 BTC 的天价费。理解 vByte，正是为了能看懂钱包到底替你算了什么。

### ② vByte 是怎么算出来的：权重、见证折扣、4 倍计权

SegWit 之后，交易数据分成两部分：**基础数据**（版本、输入输出、锁定脚本……）和**见证数据**（witness，主要是签名和公钥）。协议规定见证在计算大小时**只按四分之一计权**——这就是"见证折扣"。于是引入两个量：

- **权重（weight）= 基础字节 × 4 + 见证字节 × 1**
- **虚拟字节（vByte）= 权重 ÷ 4**

举个具体例子：一笔交易基础数据 100 字节、见证数据 64 字节。权重 = 100×4 + 64×1 = **464**，vByte = 464 ÷ 4 = **116 vB**。注意：同样 64 字节，若它在基础区会算 64 vB，挪到见证区只算 64÷4 = **16 vB**——**省了 48 vB**。所以**见证里的签名越多，折扣省得越多**，这正是 SegWit/Taproot 更便宜的数学根源。

> 为什么偏偏是"4 倍"？这是 SegWit 软分叉时定下的折中：既给见证数据足够折扣以鼓励迁移，又把区块的"权重上限"定在 **400 万**（= 旧的 100 万字节 × 4），让老节点眼中的"1 MB 限制"和新节点眼中的"4 M 权重"恰好兼容（阶段 8.2）。

### ③ 拆解一笔交易的 vByte：经验数字

记住几个粗略数字，就能心算大多数交易（够建立直觉）：

- 一个**传统（legacy）输入** ≈ **148 vB**；一个 **SegWit（P2WPKH）输入** ≈ **68 vB**——省一半还多。
- 一个 **Taproot（P2TR）输入** ≈ **57 vB**，更省；一个普通输出 ≈ **31 vB**（P2TR 输出 ≈ 43 vB）。
- 交易还有约 **10 vB** 的固定开销（版本、locktime、计数字段等）。

于是一笔"2 输入 2 输出"的 SegWit 交易 ≈ 68×2 + 31×2 + 10 ≈ **208 vB**；同样结构若用 legacy 地址 ≈ 148×2 + 31×2 + 10 ≈ **368 vB**——**贵了近八成**。手续费 = 它 × 当前费率（如 20 sat/vB 时，前者约 4160 聪）。

### ④ 省费三招（呼应前几节）

- **用 SegWit / Taproot 地址**：享见证折扣，等价交易的 vByte 直接小一截（②③ 已算）。
- **管理好 UTXO，别囤一堆碎币**：输入越多、交易越大越贵。网络空闲、费率低（如个位数 sat/vB）时，可以把一堆零钱**合并（consolidate）**成一两个大 UTXO，省下将来动它们的钱（阶段 4.1 的"粉尘"问题）。
- **挑费率低的时段**：费率是一场**竞价**——矿工优先打包出价高的交易，拥堵时贵、空闲时白菜价。用 mempool 看实时费率，不急的转账等空闲时段，能差出十几倍。
`,

  demo: "fee-estimator",

  analogy: `
寄快递按**体积重量**收费，而不是按里面东西值多少钱。比特币也一样：贵不贵看交易这个"包裹"有多大，跟你转多少钱无关。

输入就像往箱子里多塞一件带说明书的零件（每件都占地方），输出是收件地址；而 SegWit 像把那叠厚厚的说明书改成**计费时只算四分之一**的轻薄附页——同样的内容，体积小了，运费自然降。
`,

  misconceptions: [
    "“转的钱多，手续费就高。” —— 不。手续费看交易的 vByte 大小，与金额无关。",
    "“交易大小就是它的字节数。” —— SegWit 后用 vByte（虚拟字节）计费：权重 = 基础×4 + 见证×1，再÷4，所以 vByte 通常小于原始字节数。",
    "“输入多少个 UTXO 不影响费用。” —— 影响很大。每个输入都带钥匙数据，输入越多交易越大、越贵；一个 legacy 输入约 148 vB。",
    "“换成 bc1 地址省费是错觉。” —— 是真的省。见证折扣让 SegWit/Taproot 交易的 vByte 更小，同费率下更便宜。",
    "“手续费是协议里单独的一个字段。” —— 没有这个字段。手续费 = 输入总额 − 输出总额，由钱包靠调整找零来设定；漏写找零会让差额全变手续费。",
  ],

  quiz: [
    {
      q: "比特币手续费由什么决定？",
      options: ["转账金额", "费率 × 交易大小（vByte）", "收款人数量", "钱包品牌"],
      answer: 1,
      explain: "费用 = 费率 × vByte，和金额无关。",
    },
    {
      q: "“vByte（虚拟字节）”相比原始字节，特别在哪？",
      options: ["完全一样", "见证数据（签名）按四分之一计权，于是更小", "按金额计算", "由矿工随意定"],
      answer: 1,
      explain: "见证折扣：权重 ÷ 4 得到 vByte，签名越多越省。",
    },
    {
      q: "下面哪种做法**不能**省手续费？",
      options: ["改用 SegWit/Taproot 地址", "在低费率时段交易", "把很多碎 UTXO 一次性都当输入花", "空闲时合并零钱"],
      answer: 2,
      explain: "输入越多交易越大越贵；该在低费时合并，而非随手全花。",
    },
    {
      q: "同样一笔“2 输入 2 输出”，为什么 SegWit 比传统地址便宜？",
      options: ["矿工偏心", "SegWit 输入的见证数据享折扣，vByte 更小", "SegWit 金额更小", "传统地址要交税"],
      answer: 1,
      explain: "见证折扣让等价交易的计费大小更小。",
    },
  ],

  further: [
    { label: "mempool.space：实时费率与区块", url: "https://mempool.space/" },
    { label: "learnmeabitcoin：交易大小 / vByte", url: "https://learnmeabitcoin.com/technical/transaction/size/" },
  ],
};
