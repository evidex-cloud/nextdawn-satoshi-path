export default {
  id: "mempool-propagation",
  stage: 6,
  order: 3,
  title: "mempool 与区块传播：交易和区块怎么扩散",
  difficulty: "core",
  prereqs: ["p2p-network", "tx-size-fees"],

  oneLiner:
    "交易广播后，进入每个节点各自的“待确认池（mempool）”排队，按费率竞价等待打包；矿工总是优先挑费率最高的、装进有限的区块空间。新区块挖出后同样在网络里飞速扩散，让全网更新到最新高度。",

  intuition: `
一笔交易从"发出"到"上链"，中间住在 **mempool（待确认池）**里排队等打包。区块空间**有限**，矿工总是**按费率从高到低**挑交易往里塞，直到装满（回扣阶段 1 与 4 的费率竞价）——于是 mempool 本质是一场**实时拍卖**：你出的费率，就是你的出价。

区块挖出后又得**飞速扩散**到全网，让每个节点更新到最新高度。这一节就把"交易怎么排队、区块怎么传播"两件事讲透。

右边的演示：一池子不同费率的交易，看矿工把哪些塞进了下一个有限大小的区块。

**这一节，我们把"mempool 与区块传播"拆成四块：**

- **① mempool 是什么——每节点一份的待确认池**
- **② 费率拍卖——矿工怎么挑、什么叫"最低入场费率"**
- **③ 卡住了怎么办——驱逐、RBF 与 CPFP 加费**
- **④ 区块怎么传播——竞争出块、孤块与紧凑区块**
`,

  mechanics: `
### ① mempool 是什么：每节点一份的待确认池

mempool（memory pool，内存池）是**每个节点在自己内存里维护的一份"已验证、待确认"交易集合**。

- **它不是全局统一的。** 各节点收到交易的时机不同、配置的策略（最小费率、容量上限）也略有差异，所以**每个节点的 mempool 内容都不完全一样**。你在 mempool.space 上看到的，只是某一台节点的视角。
- **进池先过验证。** 一笔交易要进某节点的 mempool，得先通过它的完整校验（签名、无双花、费率不低于该节点的下限……）——这也是为什么 mempool 里的交易都是"合法但还没被打包"的。
- **默认有容量上限**（Bitcoin Core 默认约 **300 MB**）。装满后会触发驱逐（见③）。

### ② 费率拍卖：矿工怎么挑、什么叫"最低入场费率"

矿工构造区块时，目标是**在有限空间里把手续费收益最大化**，所以按**费率（聪/vByte）从高到低**贪心地塞交易：

- 注意是按**费率**而非**总费**排序。一笔费率高但体积小的交易，可能比一笔总费更高、却很臃肿的交易更受欢迎——因为它"每单位空间赚得更多"（vByte 的概念见阶段 4.4）。
- 塞到区块（约 **4 M 权重单位**，相当于满载几千笔）装满为止，最后挤进来那笔的费率，就成了这一块的**"最低入场费率"**：出价高于它的进了这块，低的继续等下一块。
- 所以**拥堵时费率飙升、空闲时费率跌到地板**：这是一个纯粹由供需决定的市场，没有谁在"定价"。

### ③ 卡住了怎么办：驱逐、RBF 与 CPFP

如果你的交易费率出低了，会怎样？

- **被驱逐（eviction）。** 当 mempool 占满、新来的高费交易需要腾地方时，节点会**丢弃费率最低的交易**。一笔费率过低的交易，可能久久不确认、最终被挤出多数节点的 mempool（看上去就像"消失了"，但它从未上链，可重发）。
- **RBF（Replace-By-Fee，费率替换，BIP125）：** 用**同样的输入**重新构造一笔**更高费率**的交易，去替换掉还没确认的那一笔。适合"发完才发现给少了"的付款方主动加速。
- **CPFP（Child-Pays-For-Parent，子带父）：** 当你是**收款方**、动不了那笔低费交易时，可以**花掉它的输出**再造一笔高费的"子交易"。矿工想赚子交易的高费，就必须连**父交易一起打包**——于是子交易"拉动"父交易一起确认。
- 一句话区分：**RBF 是发款人改自己的交易；CPFP 是任何能花到该输出的人，用一笔新交易把它"拖"上车。**

### ④ 区块怎么传播：竞争出块、孤块与紧凑区块

区块挖出后也要在 P2P 网络上扩散（回扣阶段 6.1），而且**越快越好**——因为延迟会直接导致分叉：

- **几乎同时出块 → 短暂分叉。** 两个矿工可能在相距很近的时刻各挖出一个合法区块，网络一时出现两条等长的链。节点们**各自先收到哪个就先认哪个**，直到下一个区块出现、把其中一条拉长。
- **收敛到工作量更多的那条。** 网络最终统一到累积工作量最多的链，另一条上的区块成为**孤块 / 陈块（orphan / stale block）**，它里面的交易（若没被新链收录）会退回 mempool 重新排队（回扣阶段 2.4、5.4）。挖出陈块的矿工白费了电——所以**降低传播延迟对矿工有真金白银的意义**。
- **紧凑区块（compact blocks，BIP152）。** 为了把延迟压到最低，节点通常不重发整块，而是只发**区块头 + 一串短交易 ID**；因为邻居的 mempool 里大概率已有这些交易，对方就能在本地把整块**几乎瞬间拼装**出来，大幅省下带宽与时间。
`,

  demo: "block-builder",

  analogy: `
mempool 像**候机大厅**，交易是等着登机的乘客，**手续费就是票价**。每班飞机（区块）座位有限，地勤先请**头等舱（高费率）**登机，普通票排后面。

票价太低的乘客可能一直等下一班、再下一班，大厅人太满时甚至被请出去（驱逐）。想插队？补差价升舱（RBF 加费）就行。
`,

  misconceptions: [
    "“全网有一个统一的 mempool。” —— 没有。mempool 是每个节点各自维护的，内容略有差异。",
    "“交易会一直在 mempool 里等到确认。” —— 不一定。费率过低、池子拥挤时可能被驱逐，需要重发或加费。",
    "“一个区块能装下任意多交易。” —— 不能。区块空间有限，这正是费率竞价的根源。",
    "“矿工按先到先得打包交易。” —— 通常按费率高低择优，不是单纯排队。",
  ],

  quiz: [
    {
      q: "交易广播后、上链前，住在哪里？",
      options: ["矿工的口袋", "每个节点各自的待确认池 mempool", "交易所账户", "区块头"],
      answer: 1,
      explain: "进 mempool 排队，等矿工择优打包。",
    },
    {
      q: "矿工往有限的区块里塞交易，依据是？",
      options: ["先到先得", "费率从高到低，直到装满", "金额从大到小", "随机"],
      answer: 1,
      explain: "高费率优先，于是每块有个“最低入场费率”。",
    },
    {
      q: "一笔费率过低的交易，可能遭遇什么？",
      options: ["立刻确认", "长时间不确认，甚至在 mempool 拥挤时被驱逐", "自动加费", "被退款"],
      answer: 1,
      explain: "可重发或用 RBF/CPFP 加费来挽救。",
    },
    {
      q: "两个矿工几乎同时出块会怎样？",
      options: ["都永久有效", "短暂分叉，网络收敛到工作量更多的那条，另一条成为孤块", "网络崩溃", "币翻倍"],
      answer: 1,
      explain: "回扣 2.4 / 5.4 的最长链收敛。",
    },
  ],

  further: [
    { label: "mempool.space：实时观察 mempool", url: "https://mempool.space/" },
    { label: "learnmeabitcoin：Mempool / 交易传播", url: "https://learnmeabitcoin.com/beginners/guide/" },
  ],
};
