// 样板课 —— 这就是"每节课的固定模板"，写新课时照这个结构填空即可。
// 字段说明见 README。prose 字段支持极简 Markdown：**加粗**、`代码`、[文字](链接)、- 列表、> 引用、空行分段。

export default {
  id: "hashing",
  stage: 3,
  order: 1,
  title: "哈希函数：比特币的指纹机器",
  difficulty: "core", // intro | core | deep
  prereqs: [],        // 例如 ["binary-basics"]，会渲染成前置链接

  oneLiner:
    "哈希函数把任意数据压成一段**固定长度**的“指纹”——同样的输入永远得到同样的指纹，改动一个字指纹就面目全非，而且没人能从指纹倒推回原文。比特币几乎每个环节都靠它。",

  // 🧠 直觉解释（不用术语，先建立心智模型）
  intuition: `
想象一台机器：你往里丢**任何东西**——一个字、一整本书、一部电影——它都吐出一串**长度固定**的字符串（比特币用的 SHA-256 永远是 256 位 = 64 个十六进制字符）。这台机器叫**哈希函数**，是比特币几乎每个零件的共同地基。

它的神奇之处在于四条性质：同样输入永远同样输出（确定性），改一个字输出就面目全非（雪崩），从指纹反推原文等于不可能（单向），两个输入撞出同一指纹也几乎不可能（抗碰撞）。把这四条记牢，比特币后面的区块、交易、挖矿、地址就都顺理成章了。

右边的实验台可以让你**亲手验证**这些性质——改一个字，看看输出怎么天翻地覆。

**这一节，我们把"哈希函数"拆成四块：**

- **① 四条魔法性质——确定、雪崩、单向、抗碰撞**
- **② SHA-256 长什么样——256 位与双重哈希**
- **③ 哈希在比特币里的五处身影——把零件粘起来的胶水**
- **④ 雪崩与单向的数字直觉——为什么穷举也撞不出来**
`,

  // ⚙️ 深入原理（愿意往深走的人看；可在课程顶部切到"只看直觉版"隐藏本节）
  mechanics: `
### ① 四条魔法性质：确定、雪崩、单向、抗碰撞

记住这四条，哈希在比特币里的每个用法都能自己推出来：

- **确定性**：同样的输入，永远得到同样的输出。今天算和十年后算，全球任何一台机器算，结果**一字不差**——这是"人人能独立验证"的前提。
- **雪崩效应**：改动输入里**哪怕一个字符**（甚至一个标点的大小写），输出就面目全非——平均约**一半的位**会翻转。你没法靠"微调输入"来"微调输出"。
- **单向性**：从输入算指纹极快；但拿着指纹想反推回输入，**没有任何捷径，只能暴力穷举**，对高熵数据等于物理上不可能。
- **抗碰撞**：几乎不可能找到两个不同输入得到同一指纹。正因如此，一个哈希才能"代表"它的原文。

> 一句话：哈希是数据的**指纹**——唯一、固定、可验证，但不可逆。

### ② SHA-256 长什么样：256 位与双重哈希

比特币的主力哈希函数是 **SHA-256**（Secure Hash Algorithm，输出 **256 位 = 32 字节 = 64 个十六进制字符**）：

- 不管你喂进 1 字节还是 1 GB，输出永远是这固定的 256 位——一个介于 0 到 2²⁵⁶−1 之间的巨大整数。2²⁵⁶ 约等于 10⁷⁷——**这个数有多大？整个可观测宇宙里的原子总数，也不过约 10⁸⁰**。
- 很多关键位置用的其实是**双重 SHA-256**（记作 SHA256d，即连算两遍 \`SHA256(SHA256(x))\`）：区块头哈希、txid、默克尔树都用它。历史上多算一遍是为了防一类"长度扩展攻击"，多一层冗余保险。
- 地址那条路略有不同：公钥先 SHA-256、再 **RIPEMD-160**（输出 160 位，更短），这一步叫 HASH160（阶段 4 详解）。

### ③ 哈希在比特币里的五处身影：把零件粘起来的胶水

哈希几乎无处不在，下面五处撑起了整个系统：

- **区块链接**：每个区块头都嵌着**上一个区块头的哈希**。改动任一旧区块，它的哈希就变，后面所有链接全部失效——这就是"链"防篡改的根源（阶段 2.1）。
- **交易 ID（txid）**：一笔交易内容做双重 SHA-256，就是它全网唯一的编号。你在浏览器里搜的就是它。
- **默克尔树**：把一个区块里成千上万笔交易两两哈希、层层向上，汇成一个**默克尔根**塞进区块头（阶段 3.3 专讲）。
- **工作量证明（挖矿）**：矿工不停改区块头里的 nonce、反复哈希，直到**区块头哈希作为一个数小于目标值**。因为哈希不可预测、雪崩，只能一次次试——这就是"工作量"的本质（阶段 2.3 / 5）。
- **地址**：公钥经 SHA-256 + RIPEMD-160 得到公钥哈希，再编码成你常见的地址（阶段 4）——多一层哈希，公钥在花费前都不必暴露。

### ④ 雪崩与单向的数字直觉：为什么穷举也撞不出来

为什么大家敢说"哈希工程上不可逆、不会碰撞"？用数字感受一下规模：

- **穷举反推有多难**：要从一个 256 位指纹猜回原文，平均要试 2²⁵⁵ 次。即便拿全球比特币算力（约 10²¹ 次/秒，阶段 2.3 的 EH/s 级别）来跑，也要远超**宇宙年龄**才能跑完——这就是"单向"在物理上的含义。
- **碰撞有多罕见**：靠生日攻击找一个 SHA-256 碰撞，约需 2¹²⁸ 次尝试。这个数比地球上所有沙粒还多无数倍，工程上直接视为"不会发生"。
- **雪崩的用处**：正因为改一个字就有约一半的位翻转、且结果不可预测，哈希才能**灵敏检测篡改**（改一点就全变），又**无法被反向微调**（想凑出特定指纹只能瞎试）——这两点同时支撑了防篡改和挖矿。

换句话说：**哈希是把比特币各个零件粘在一起的胶水。** 把它理解透，后面的区块、交易、挖矿都会轻松很多。
`,

  // 🔬 交互演示（对应 demos/ 下的组件 id）
  demo: "hash-lab",

  // 🪞 类比
  analogy: `
像一台**果汁机**：你放进苹果、放进香蕉、放进一整桌菜，它都打成一杯果汁。打果汁很快，但你**没法从果汁还原出原来的水果**——这就是单向性。

或者像**指纹**：每个人的指纹独一无二、终生固定，可以用来核验身份；但你**没法靠一枚指纹"长出"一个人**。
`,

  // ⚠️ 常见误解（逐条纠正）
  misconceptions: [
    "“哈希就是加密。” —— 不是。加密是为了之后能**解密**还原；哈希是**不可逆**的，根本不存在“解哈希”这种操作。",
    "“不同输入可能撞出相同哈希，所以不可靠。” —— 理论上碰撞存在，但对 SHA-256 而言，概率小到用整个宇宙的算力跑到宇宙终结也撞不出来，工程上视为不会发生。",
    "“知道了哈希，就能猜出原文。” —— 只能靠暴力穷举。对一个高熵的输入（比如一个随机私钥），这在物理上不可行。",
  ],

  // ✅ 自测（点选即时反馈）
  quiz: [
    {
      q: "把输入里的一个字符改一下，输出的哈希会怎样？",
      options: [
        "基本不变，只差一两位",
        "完全不同，大约一半的位翻转",
        "长度会变短",
        "无法预测会不会变",
      ],
      answer: 1,
      explain: "这叫**雪崩效应**：微小的输入改动导致输出剧烈变化。正因如此，哈希既能灵敏地检测篡改，又无法被“微调”。",
    },
    {
      q: "下列哪一个**不是**哈希函数的性质？",
      options: [
        "确定性（同输入同输出）",
        "可逆（能从哈希还原出原文）",
        "固定长度输出",
        "抗碰撞",
      ],
      answer: 1,
      explain: "哈希是**单向**的，不可逆。这正是它和“加密”最大的区别。",
    },
    {
      q: "比特币挖矿，本质上是在反复做什么？",
      options: [
        "解密一段密文",
        "不停尝试 nonce，直到区块头的哈希小于目标值",
        "把交易加密起来",
        "计算每笔交易的手续费",
      ],
      answer: 1,
      explain: "工作量证明就是**反复哈希**、不断换 nonce，直到撞出一个足够小的哈希。因为哈希不可预测，没有捷径，只能试——这就是“工作量”。",
    },
  ],

  // 📚 延伸
  further: [
    { label: "比特币白皮书（中本聪原文 PDF）", url: "https://bitcoin.org/bitcoin.pdf" },
    { label: "learnmeabitcoin：Hash function（图文交互）", url: "https://learnmeabitcoin.com/technical/cryptography/hash-function/" },
  ],
};
