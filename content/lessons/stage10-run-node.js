export default {
  id: "run-node",
  stage: 10,
  order: 1,
  title: "跑你自己的全节点：成为网络的一份子",
  difficulty: "deep",
  prereqs: ["node-types", "p2p-network"],

  oneLiner:
    "运行一个全节点（如 Bitcoin Core），就是亲自下载并验证整条链——从此你不再信任任何人对“账本现在什么样”的说法，而是自己的软件说了算。这是“Don’t trust, verify”最彻底的实践，也是你在比特币里最大的一份主权。",

  intuition: `
学了这么多，最有"主权感"的一步，是**自己跑一个全节点**。

它做的事，正是你前面学的全部规则的合体：从创世区块开始**逐块亲自验证**（签名、双花、PoW、发行量……），任何不合规的东西一律**拒绝、不转发**，再把验证过的数据**转发**给邻居，成为 P2P 网络的一员。

你得到的回报不是钱，而是：**再也不必相信任何第三方告诉你"链上发生了什么"**——余额、确认、规则，全由你自己的软件说了算。这还顺带改善隐私（不再把你的地址交给别人的服务器去查）。

右边切换"剪枝模式"，看看跑一个节点要多少存储、能得到什么。

**这一节，我们把"跑全节点"拆成四块：**

- **① 全节点到底在做什么——验证、执行、转发**
- **② 怎么真正跑起来——下载、配置、初次区块下载（IBD）**
- **③ 完整 vs 剪枝——600 GB 还是 几 GB，验证强度一样**
- **④ 跑节点意味着什么——主权、隐私，与你的"一票"**
`,

  mechanics: `
### ① 全节点在做什么：验证、执行、转发

一个全节点不是"下载器"，而是一台**独立的规则裁判机**。它对收到的每一个区块、每一笔交易都做三件事：

- **验证**：检查每个签名是否有效、每笔输入花的 UTXO 是否还在 UTXO 集里（防双花）、PoW 是否达标、区块奖励有没有超发、脚本是否通过……一条规则不过就整体拒绝。
- **执行**：不合规的东西，无论是谁发来的，节点**一律丢弃、绝不转发**——这就是共识规则被"自下而上"强制执行的物理现场（阶段 6.4 / 8.1）。
- **转发**：验证通过的交易和区块，转给它的对等节点（peers），让有效数据在 P2P 网络里扩散（阶段 6.3）。

跑起来后，可以用 \`bitcoin-cli getblockchaininfo\` 看它的整体状态，\`getpeerinfo\` 看它连了哪些邻居——你会亲眼看到自己已经是网络的一个节点。

### ② 怎么真正跑起来：下载、配置、IBD

参考实现是 **Bitcoin Core**（绝大多数全节点跑的就是它）。最小路径其实只有三步：

- **第 1 步 · 装**：从 \`bitcoincore.org\` 下载对应平台的包，**务必校验 GPG 签名与哈希**（确认你装的没被掉包）。
- **第 2 步 · 配**：写一个 \`bitcoin.conf\`，放在数据目录里。常见几行：\`server=1\`（开 RPC，下一节用）、\`txindex=1\`（建全量交易索引，区块浏览器要用）、\`dbcache=4096\`（给更大内存缓存，IBD 能快不少）。
- **第 3 步 · 启**：跑 \`bitcoind\`（后台守护进程）或图形版 \`bitcoin-qt\`。第一次启动会进入**初次区块下载（IBD）**——从创世区块 0 开始，把约 90 万个区块、累计数百 GB 的历史**逐块下载并从头验证一遍**。这通常要**数小时到一两天**，取决于硬盘速度（强烈建议 SSD）、带宽和 \`dbcache\`。
- **校验进度**：IBD 期间反复跑 \`bitcoin-cli getblockcount\`，看高度往上爬；\`getblockchaininfo\` 里的 \`verificationprogress\` 接近 \`1.0\` 时就快同步完了。

为什么非得从头验证、不能直接信任一个"快照"？因为一旦你接受别人给的状态，你就把"账本现在什么样"的信任交还给了对方——而跑节点的全部意义，正是把这份信任收回到自己手里。

### ③ 完整 vs 剪枝：存储差很多，验证强度一样

到 2026 年，完整链历史已超过 **600 GB** 并持续增长。怕硬盘装不下？比特币给了一个对个人极友好的选项——**剪枝（pruning）**：

- 在 \`bitcoin.conf\` 里写 \`prune=550\`（单位 MB，550 是允许的最小值），节点照样**从创世块全量验证每一个区块**，但验证完旧区块就**丢弃**，本地只留最近的一小段，磁盘占用可压到 **几 GB**。
- **关键点：验证强度完全不打折**。剪枝节点和完整节点一样亲自校验了整段历史，得到的安全保证一模一样——区别只是它不再保留全部历史供别人下载，也因此**不能再跑某些需要回溯旧区块的功能**（比如对历史区块建 \`txindex\`、当别人的同步源）。
- 所以选择很简单：想给网络做"档案馆"、或要跑区块浏览器/全量索引 → 完整节点；只想给自己一个去信任的钱包后端 → 剪枝节点足矣（阶段 6.2）。

### ④ 跑节点意味着什么：主权、隐私、你的"一票"

为什么值得折腾？因为它一次性给你三样别处买不到的东西：

- **主权（don't trust, verify）**：你的钱包余额、一笔交易有几个确认、某条规则是否被遵守，从此都由**你自己的软件**回答，而不是某家公司的服务器。这是整门课"自己验证"精神最彻底的落地。
- **隐私**：用轻钱包查余额时，你其实把"我关心这些地址"告诉了对方的服务器；连到你自己的节点查，这层泄露就消失了（阶段 9.1）。
- **共识里的"一票"**：节点**只执行你选择的规则**、拒绝任何不合规的区块。当年的扩容之争（阶段 8.1）证明：真正决定"什么是比特币"的，不是矿工的算力，而是**成千上万个验证节点共同坚持的规则**。你跑一个节点，就是为你认同的规则投下最实在的一票（阶段 6.4 / 8.1）。
`,

  demo: "node-setup",

  analogy: `
跑全节点，像**自己保留一份完整账本原件、并逐页亲自核对**：别人报给你的任何余额、任何"链上刚发生了什么"，你都能当场复核，再不必看谁的脸色、信谁的一面之词。
`,

  misconceptions: [
    "“跑全节点就是挖矿。” —— 不是。验证 ≠ 挖矿；绝大多数全节点根本不挖矿，只做验证与转发，普通电脑就能跑（阶段 6.2）。",
    "“跑节点需要昂贵的专业设备。” —— 一台普通电脑加一块硬盘即可；剪枝模式（prune=550）只需几 GB，门槛很低。瓶颈通常只是 IBD 那几小时到一两天的同步时间。",
    "“跑节点能帮我直接赚钱。” —— 没有任何区块奖励或手续费；它换来的是主权、隐私与对第三方的零信任，这些才是它的回报。",
    "“用钱包 App 看余额，就等于我自己验证了。” —— 不。除非那个钱包连的是你自己的节点，否则你仍在信任对方服务器告诉你的数字。",
    "“剪枝节点是‘阉割版’，安全性更低。” —— 不。它和完整节点一样从创世块全量验证，安全保证完全相同；只是丢弃旧区块、不能再当别人的同步源而已。",
  ],

  quiz: [
    {
      q: "运行全节点最核心的价值是？",
      options: ["挖矿赚币", "亲自验证一切，不再信任任何第三方的链上说法", "加快网速", "获得空投"],
      answer: 1,
      explain: "Don’t trust, verify 的最彻底实践。",
    },
    {
      q: "“剪枝节点”与完整节点相比？",
      options: ["验证更弱", "验证强度相同，只是丢弃旧区块、省存储", "不能联网", "需要矿机"],
      answer: 1,
      explain: "全量验证不打折，门槛却低很多（回扣 6.2）。",
    },
    {
      q: "“初次区块下载（IBD）”在做什么？",
      options: ["挖第一个区块", "从创世开始逐块验证整段历史", "下载钱包", "注册账户"],
      answer: 1,
      explain: "亲自校验全历史后，你才真正去信任。",
    },
    {
      q: "跑节点如何体现你在共识里的“一票”？",
      options: ["你能投票改规则", "你的节点执行你选择的规则，不接受不合规的区块", "你能多挖币", "你能否决交易"],
      answer: 1,
      explain: "规则自下而上被每个验证者强制执行（回扣 6.4 / 8.1）。",
    },
  ],

  further: [
    { label: "Bitcoin.org：运行一个全节点", url: "https://bitcoin.org/zh_CN/full-node" },
    { label: "Bitcoin Core 官网", url: "https://bitcoincore.org/" },
  ],
};
