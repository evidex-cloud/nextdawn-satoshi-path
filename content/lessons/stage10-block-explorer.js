export default {
  id: "block-explorer",
  stage: 10,
  order: 3,
  title: "读懂区块浏览器：把所学连起来",
  difficulty: "deep",
  prereqs: ["utxo", "tx-size-fees", "segwit"],

  oneLiner:
    "区块浏览器（如 mempool.space）把链上数据可视化。学到这里，你已经能看懂一笔真实交易的几乎每个部件：输入/输出、找零、手续费与 vB、确认数、见证数据……每一项都对应你前面学过的某一课。",

  intuition: `
区块浏览器把抽象的链上数据，摊成你能点开细看的页面：区块、交易、地址。而**学到这里，你已经能读懂其中几乎每一个字段**——这就像一次"期末检阅"：

- **txid**：交易指纹（阶段 5.1 / 8.2）
- **输入**：花掉的是哪些 UTXO，附带解锁脚本/签名（阶段 4.1 / 4.2）
- **输出**：付款 + 找零，各带一把锁、对应不同地址类型（阶段 4.1 / 4.3）
- **手续费 / vB**：输入减输出，以及虚拟字节大小（阶段 4.1 / 4.4）
- **见证**：隔离出去的签名（阶段 8.2）
- **确认数**：被多少区块埋住、有多不可逆（阶段 2.2 / 5.4）

右边点开一笔交易的各个部件，看它是什么、对应你学过的哪一课。

> 一个提醒：公共浏览器是**第三方的视角**。要真正去信任，用你自己节点的浏览器/RPC；而在公共浏览器上查自己的地址，也可能泄露隐私（阶段 9.1）。

**这一节，我们把"读浏览器"拆成四块：**

- **① 逐字段读一笔交易——把每个部件对回它的那一课**
- **② 顺藤摸瓜——输入往回、输出往后，链上分析的基础**
- **③ 三种页面——交易、区块、地址各看什么**
- **④ 公共浏览器的局限——第三方视角与隐私，以及自托管**
`,

  mechanics: `
### ① 逐字段读一笔交易

打开 mempool.space，搜任意一个 txid，你会看到一张交易页。从上到下逐个字段读，每一个都能对回前面某一课：

- **txid**：整笔交易内容的哈希指纹，全网唯一（阶段 5.1）。注意 SegWit 后还有个 \`wtxid\`，把见证也算进去（阶段 8.2）。
- **输入（inputs）**：每个输入指向一枚被花掉的 UTXO（写作 \`前一笔TXID:index\`），并附解锁数据。点开能看到它解锁的是哪种脚本、金额多少（阶段 4.1 / 4.2）。
- **输出（outputs）**：通常两个——付款 + 找零。**怎么认出找零？** 看地址类型和金额：找零往往回到与发送方同类型的地址、金额是个"零头"。每个输出各带一把锁（scriptPubKey），对应 \`1...\`（P2PKH）、\`3...\`（P2SH）、\`bc1q...\`（原生 SegWit）、\`bc1p...\`（Taproot）等地址类型（阶段 4.3）。
- **手续费 / vB**：浏览器替你算好的 \`手续费 = 输入总额 − 输出总额\`，并给出 **vB（虚拟字节）** 大小和 **费率（sat/vB）**。验证一下：费率 ≈ 手续费 ÷ vB（阶段 4.1 / 4.4）。
- **见证（witness）**：SegWit 交易里，签名被挪到了单独的见证区，按"权重折扣"计费（这正是它便宜的原因，阶段 8.2）。
- **确认数（confirmations）**：这笔交易下面埋了多少个区块。0 = 还在 mempool；1 = 刚进块；6+ = 实务上钉死（阶段 2.2 / 5.4）。

### ② 顺藤摸瓜：往回与往后

浏览器真正强大的地方，是每个输入/输出都是**可点的链接**：

- **点某个输入** → 跳到"创造这枚 UTXO 的那笔交易"，可以一路往**过去**回溯资金来源。
- **点某个输出** → 如果它已被花掉，跳到"后来花它的那笔交易"，往**未来**追踪资金去向；若显示 \`unspent\`，说明它还是一枚活的 UTXO。
- 这种前后追踪，正是**链上分析（chain analysis）**的基础——也正因为如此，复用地址、把多枚 UTXO 凑在一笔交易里，都会泄露关联信息（阶段 9.1 的隐私课）。

### ③ 三种页面：交易、区块、地址

浏览器主要有三类页面，各看不同东西：

- **交易页**：就是上面逐字段拆的那张。
- **区块页**：搜一个高度（如 \`840000\`）或区块哈希，能看到它的头部字段——时间戳、矿工（coinbase 留的标记）、难度、默克尔根、装了多少笔交易、总手续费（阶段 5.1）。
- **地址页**：输入一个地址，列出它收过/花过的所有交易和当前余额。⚠ 但要记住：链上没有"账户"，这个"余额"是浏览器**把该地址相关的 UTXO 加总**算给你看的（阶段 4.1）——而你一查，就等于告诉这个公共服务"你关心这个地址"。

### ④ 公共浏览器的局限与自托管

公共浏览器方便，但有两条根本局限，正好呼应整门课的主线：

- **它是第三方视角**：mempool.space 给你看的是**它的节点**眼里的链。99% 情况没问题，但"最权威"的永远是你自己验证过的节点——别把浏览器的显示当成绝对真相。
- **查询会泄露隐私**：在公共浏览器查自己的地址/交易，等于向它暴露"这些是我关心的"，可被关联分析（阶段 9.1）。
- **解法：自托管**。mempool.space、BTC RPC Explorer 等都是**开源**的，可以**架在你自己的全节点上**（接阶段 10.1 的节点 + 10.2 的 RPC）。这样既拿回了权威，又不泄露任何查询——去信任与隐私一举两得。到这里，前面所有抽象概念都成了你能亲眼点开、逐一指认的东西。
`,

  demo: "explorer",

  analogy: `
区块浏览器像一台**X 光机**，把每笔交易的骨架照得清清楚楚。

走完这条路，你已经从面对"看不懂的乱码"，变成了能逐块读片的"放射科医生"——每根骨头叫什么、连着哪里，你都门儿清。
`,

  misconceptions: [
    "“浏览器显示的就是绝对真相。” —— 它是某个第三方的视角；最权威的是你自己验证过的节点。",
    "“在公共浏览器查自己的地址没风险。” —— 可能向该服务暴露‘你关心这些地址’，损害隐私（阶段 9.1）。",
    "“凭 txid 就能查到我的真实身份。” —— 只有当它在别处被关联到你时才会；链本身只有假名。",
    "“确认数是浏览器定的。” —— 它只是反映这笔交易被多少个区块埋住，由链决定。",
  ],

  quiz: [
    {
      q: "一笔交易的手续费，在浏览器上怎么算出来？",
      options: ["看金额百分比", "输入总额 − 输出总额", "固定值", "由浏览器随机给"],
      answer: 1,
      explain: "差额归矿工，≈ 费率 × vB（回扣 4.1 / 4.4）。",
    },
    {
      q: "在浏览器里点开一个“输出”，能顺藤看到？",
      options: ["矿工是谁", "它后来被哪笔交易花掉（或还未花）", "你的身份", "币价"],
      answer: 1,
      explain: "UTXO 的来龙去脉，也是链上分析的基础（9.1）。",
    },
    {
      q: "想要去信任又护隐私地浏览链上数据，最好？",
      options: ["用最大的公共浏览器", "把开源浏览器架在自己的节点上", "问交易所", "相信截图"],
      answer: 1,
      explain: "自己的节点 = 权威 + 隐私。",
    },
    {
      q: "公共区块浏览器的本质局限是？",
      options: ["数据是假的", "它是第三方视角，且查询可能泄露你的隐私", "速度慢", "要收费"],
      answer: 1,
      explain: "最权威、最私密的，永远是你自己的节点。",
    },
  ],

  further: [
    { label: "mempool.space（可自托管的开源浏览器）", url: "https://mempool.space/" },
    { label: "learnmeabitcoin：浏览真实交易", url: "https://learnmeabitcoin.com/explorer/" },
  ],
};
