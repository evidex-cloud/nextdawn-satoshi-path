export default {
  id: "merkle-tree",
  stage: 3,
  order: 3,
  title: "默克尔树：把一万笔交易压成一个指纹",
  difficulty: "core",
  prereqs: ["hashing"],

  oneLiner:
    "默克尔树把一个区块里的成千上万笔交易，两两哈希、层层向上，最终汇成一个“默克尔根”塞进区块头。它让你能用极少的数据，证明“某笔交易确实在这个区块里”，而不必下载整个区块。",

  intuition: `
一个区块可能装着上万笔交易。怎么用**一个**小小的指纹代表全部交易，还能在不下载整个区块的前提下，高效地证明"某笔交易确实在里面"？答案是**默克尔树**——一种用哈希层层向上汇总的结构。

它的做法很简单（又是哈希）：每笔交易各自哈希成"叶子"，两两配对再哈希成上一层，如此层层向上，最后只剩**一个**指纹——**默克尔根**，写进区块头。由此长出两个超能力：改动任一交易会**一路传导到根**（防篡改），而证明单笔交易在内只需一条**约 log₂(n) 长的路径**（轻量证明）。这第二点，正是手机轻钱包能存在的根基。

右边的演示：改一个叶子看根怎么变，再点"证明某笔在树里"，看它只需要哪几个哈希。

**这一节，我们把"默克尔树"拆成四块：**

- **① 怎么搭起来——从叶子两两哈希到一个根**
- **② 超能力一：牵一发动全身——防篡改怎么实现**
- **③ 超能力二：默克尔证明——log₂(n) 个哈希证明包含**
- **④ 它撑起了什么——SPV 轻钱包与实现细节**
`,

  mechanics: `
### ① 怎么搭起来：从叶子两两哈希到一个根

以一个装了 4 笔交易（Tx A、B、C、D）的小区块为例，亲手搭一棵树：

- **第 0 层（叶子）**：各自哈希——H_A = hash(A)、H_B = hash(B)、H_C = hash(C)、H_D = hash(D)。
- **第 1 层**：相邻两两拼接再哈希——H_AB = hash(H_A + H_B)、H_CD = hash(H_C + H_D)。
- **第 2 层（根）**：H_ABCD = hash(H_AB + H_CD)，这就是**默克尔根**，被写进区块头。
- 每往上一层，节点数减半，所以 n 笔交易的树高约 **log₂(n)**：1 万笔交易也只有约 14 层。
- **奇数怎么办？** 某层若是奇数个节点，比特币把**最后一个和它自己配对**再哈希（不是补零），凑成偶数继续向上。

### ② 超能力一：牵一发动全身——防篡改怎么实现

默克尔根之所以能"代表全部交易"，靠的是哈希的雪崩效应一路向上传导：

- 假设有人偷改了 Tx C 的金额 → H_C 变 → H_CD 变 → 根 H_ABCD 变。
- 而根被锁在区块头里，区块头又参与挖矿和链接（阶段 2.1）——根一变，这个块的头哈希就变，与下一块对不上，**篡改当场暴露**。
- 所以矿工**没法在不重做工作量的前提下，偷换区块里的任何一笔交易**。默克尔根把"区块体里上万笔交易"压成区块头里 32 字节的一个承诺。

### ③ 超能力二：默克尔证明——log₂(n) 个哈希证明包含

这是默克尔树最实用的地方。要证明 **Tx C 在这个区块里**，你不需要 A、B、D 的内容，只需一条**默克尔证明**：

- 给你两个"兄弟指纹"：**H_D**（C 的邻居）和 **H_AB**（CD 的邻居）。
- 你自己算：H_C = hash(C) → H_CD = hash(H_C + H_D) → 根 = hash(H_AB + H_CD)。
- 算出的根若和区块头里的根**完全一致**，就证明 C 确实在树里——而你只用了 **2 个哈希**，没碰其他任何交易。
- 一般地：n 笔交易只需约 **log₂(n) 个兄弟哈希**。1 万笔交易 → 约 14 个哈希（几百字节）就能证明包含，对比下载整个 1–2 MB 区块，省了几千倍。
- **为什么非得是"树"？** 若把所有交易拼一起只哈希一次，验证单笔就得拿到全部交易重算。树形结构才换来了这条 log₂(n) 的捷径。

### ④ 它撑起了什么：SPV 轻钱包与实现细节

默克尔证明直接催生了**轻钱包（SPV，简单支付验证）**：

- 手机钱包不存整条链（数百 GB），只存每个区块的**区块头**（含默克尔根，每个仅 80 字节）。
- 要确认"我的交易被打包了"，向全节点**要一个默克尔证明**，自己算到根、和持有的区块头里的根一对，就信了——下载量极小（阶段 6 细讲节点类型）。
- 代价：SPV 只验证"交易被包含 + 工作量足够"，不像全节点那样验证每条规则，所以信任假设略强。
- **几个实现细节**：树用**双重 SHA-256**；奇数节点和自己配对（见 ①，这曾导致过 CVE-2012-2459 漏洞，需小心去重）；区块头里存的就是这个根。这些阶段 4、5 会再碰到。
- **一句话定位**：哈希是"一个东西的指纹"，默克尔树是"一堆东西的、可局部验证的总指纹"。
`,

  demo: "merkle-tree",

  analogy: `
想象一份**层层汇签的考勤表**：每个小组先把本组名单签个总名，几个小组的总名再汇签成科室名，最后汇成全公司**一个总签**，贴在门口。

- 只要有人偷改了某个组员的记录，那个组的总名就变，一路传到门口的总签——一眼穿帮。
- 而要证明"某位员工那天确实在场"，你不用搬出全公司名单，只需要他那条**汇签路径上的几个签名**，就能一路核到门口的总签。
`,

  misconceptions: [
    "“默克尔根能还原出所有交易。” —— 不能。它是单向哈希的逐层汇总，只能用来校验，不能反推出原始交易。",
    "“验证一笔交易在区块里，必须下载整个区块。” —— 不必。用默克尔证明，只需该交易到根的几个兄弟哈希（约 log₂(n) 个），1 万笔也只要约 14 个。",
    "“默克尔证明能告诉你账户余额或交易内容。” —— 不能。它只回答“这笔交易在不在这个区块里”这一个是非问题，不揭示其他交易。",
    "“默克尔树是比特币发明的。” —— 不是。它由 Ralph Merkle 在 1979 年提出，比特币把它用在了区块结构里。",
  ],

  quiz: [
    {
      q: "默克尔根是怎么得到的？",
      options: ["把所有交易编号相加", "交易两两哈希、层层向上，最终汇成一个指纹", "随机生成", "由矿工指定"],
      answer: 1,
      explain: "逐层配对哈希，最终一个根，写进区块头。",
    },
    {
      q: "默克尔树最大的实用价值是？",
      options: ["压缩交易省空间", "用极少的数据（约 log₂(n) 个哈希）证明某笔交易被包含", "加密交易内容", "加快挖矿"],
      answer: 1,
      explain: "这就是默克尔证明，撑起了轻钱包 SPV。",
    },
    {
      q: "改动区块里某一笔交易，会怎样？",
      options: ["只影响那一笔", "它的叶子指纹一路向上改变，默克尔根随之改变", "默克尔根不变", "需要全网投票"],
      answer: 1,
      explain: "牵一发而动全身——根变了，区块头就对不上，篡改暴露。",
    },
    {
      q: "手机轻钱包（SPV）靠什么确认交易被打包？",
      options: ["下载整条区块链", "只存区块头，再用默克尔证明核到根", "打电话问矿工", "完全信任交易所"],
      answer: 1,
      explain: "区块头里的默克尔根加一个简短证明，下载量极小。",
    },
    {
      q: "证明“某笔交易在一个含 n 笔交易的区块里”，大约需要几个哈希？",
      options: ["n 个", "约 log₂(n) 个兄弟哈希", "1 个", "n² 个"],
      answer: 1,
      explain: "只需从该交易到根这条路径上的兄弟指纹，约 log₂(n) 个；1 万笔交易也只要约 14 个。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Merkle root（图文）", url: "https://learnmeabitcoin.com/technical/block/merkle-root/" },
    { label: "维基百科：Merkle tree", url: "https://en.wikipedia.org/wiki/Merkle_tree" },
  ],
};
