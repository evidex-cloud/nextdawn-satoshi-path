export default {
  id: "block-header",
  stage: 5,
  order: 1,
  title: "区块头：被反复哈希的 80 个字节",
  difficulty: "core",
  prereqs: ["mining", "hashing", "merkle-tree"],

  oneLiner:
    "挖矿到底在哈希什么？答案是一个仅 80 字节的“区块头”——它打包了版本、上一区块哈希、默克尔根、时间戳、目标难度和一个 nonce。矿工就是不停改 nonce、对这 80 字节做双重 SHA-256，直到结果小于目标值。",

  intuition: `
阶段 2 说挖矿是"不停换 nonce 试哈希"。现在揭开盖子：矿工反复哈希的，并不是整个区块（那可能上兆字节），而是一个紧凑的 **80 字节区块头**。

区块头里塞了 6 样东西，每一样我们前面都见过：版本号、上一区块哈希（把本块链到前一块，阶段 2.1）、默克尔根（全部交易的总指纹，阶段 3.3）、时间戳、目标 \`bits\`（当前难度的"及格线"，下一节）、以及 \`nonce\`（唯一能随便改的"试错旋钮"）。

挖矿就是：固定前面那些，**疯狂地改 \`nonce\`，对这 80 字节做两次 SHA-256**，直到算出的哈希**小于目标值**。改一个 \`nonce\`、哈希一次、不达标、再换……每秒亿万次。

右边的演示把这 6 个字段摆出来，让你真的去挖：改难度，看要试多少次才撞中。

**这一节，我们把"区块头"拆成四块：**

- **① 80 字节装了什么——逐字段的长度与含义**
- **② 为什么只哈希这 80 字节——默克尔根的承诺作用**
- **③ 挖矿循环的内层——SHA256d、32 位 nonce 与 extranonce**
- **④ 算力与能量——这 80 字节如何把账本"焊死"**
`,

  mechanics: `
### ① 80 字节装了什么：逐字段拆解

区块头是**固定 80 字节**，按顺序排着 6 个字段（都以小端序存储，回扣阶段 4.5）：

- **版本（4 字节）**：声明规则版本，也用于发信号（如 BIP9 软分叉投票）。
- **上一区块头哈希（32 字节）**：前一个区块头的 SHA256d。这一字段把区块**串成链**（阶段 2.1）。
- **默克尔根（32 字节）**：本区块全部交易两两哈希、层层归并出的**唯一总指纹**（阶段 3.3）。
- **时间戳（4 字节）**：矿工填的 Unix 时间，须大致合理（规则限制它不能太超前、且要大于前 11 块的中位时间）。
- **目标 \`bits\`（4 字节）**：把 256 位的目标值压缩进 4 字节的紧凑编码——这是"及格线"（下一节展开）。
- **\`nonce\`（4 字节）**：32 位的纯试错字段。

32 + 32 这两个哈希就占了 64 字节，是区块头的主体；其余 16 字节是版本、时间、\`bits\`、\`nonce\`。

### ② 为什么只哈希这 80 字节：默克尔根的承诺

一个区块可能有几千笔交易、上兆字节，矿工却只对 80 字节反复哈希——凭什么这等价于"为整个区块工作"？关键在**默克尔根**：

- 它是把本区块**全部交易**两两配对、做 SHA256d、再把结果继续两两归并，最终收敛成的**一个 32 字节根**（阶段 3.3）。
- 改动**任何一笔**交易（哪怕一个聪），都会顺着这棵树一路改到根 → 默克尔根变 → 区块头变 → 之前所有 \`nonce\` 试错全部作废、必须重挖。
- 所以哈希这 80 字节，**等价于对整个区块内容的承诺**：你没法在不重挖的前提下偷换交易。这把"工作量"和"区块的全部内容"牢牢绑定在了一起。

### ③ 挖矿循环的内层：SHA256d、32 位 nonce 与 extranonce

- **双重 SHA-256（SHA256d）**：比特币算的是 \`SHA256(SHA256(头))\`。用两层是为防御一类针对单层 SHA-256 的"长度扩展攻击"，纯属保守的安全冗余。
- **\`nonce\` 只有 32 位 ≈ 43 亿种**。听起来很多，但现代一台矿机每秒就能做上百万亿次哈希——**几毫秒就把 43 亿个 \`nonce\` 全试完了**还没中。怎么办？
- **改别的可变位制造"新的 80 字节"**：矿工会微调**时间戳**，或更常见地改 coinbase 交易里一段叫 **extranonce** 的数据——这会改变默克尔根，从而让整个 \`nonce\` 空间"重置"、可以再扫一遍（阶段 5.5）。本质上，挖矿是在一个比 43 亿大得多的搜索空间里找一个达标的头。
- 一个实务结构：矿池下发"工作模板"，矿工各自扫 \`nonce\` + extranonce，找到满足**较低份额难度**的头就上交"share"证明出力（阶段 5.3）。

### ④ 算力与能量：这 80 字节如何把账本"焊死"

- **算力（hashrate）= 全网每秒能做多少次 SHA256d**。如今以 **EH/s（每秒百亿亿次，10¹⁸）** 计——这是个天文数字。
- 每一次哈希都消耗真实电力。整条链累积的工作量，本质是**已经烧掉的能量总和**——这正是阶段 2.3 说的"账本不可逆，是因为改它的代价高到没人付得起"的物理来源。
- 把全章串起来：哈希（阶段 3.1）提供不可预测性，默克尔根（3.3）锚定全部交易，上一区块哈希（2.1）串成链，目标 \`bits\`（下一节）设定难度，而 PoW 让"写下这一页"必须烧真实算力。这 80 字节，就是这一切的汇聚点。
`,

  demo: "header-mining",

  analogy: `
区块头就像一张**包裹面单**：上面写着收件链路（上一区块哈希）、内件清单的封条（默克尔根）、寄出时间，以及一个**必须凑对的防伪编号**（nonce）。

矿工的工作，就是不停地试不同的防伪编号，直到整张面单的"指纹"恰好落在规定的极小范围内。试中了，这张面单（区块）才被全网认可、贴上链。
`,

  misconceptions: [
    "“挖矿是在哈希整个区块的所有交易。” —— 不。矿工反复哈希的是 80 字节的区块头；交易已被默克尔根浓缩进头里。",
    "“nonce 取值空间无限，总能挨个试到。” —— nonce 只有 32 位（约 43 亿），矿机几毫秒就扫完。试尽仍没中时，矿工要改时间戳或 coinbase 里的 extranonce（从而改默克尔根）来制造新的待试头。",
    "“改一笔交易不影响挖矿。” —— 会。交易变 → 默克尔根变 → 区块头变 → 之前的工作量全作废，必须重挖。",
    "“区块头里直接存着难度数值。” —— 存的是紧凑编码的 4 字节“目标(bits)”，难度是由它换算来的（下一节）。",
    "“做一次 SHA-256 就行。” —— 比特币区块头用双重 SHA-256（SHA256d），即对头做两次哈希，属安全冗余设计。",
  ],

  quiz: [
    {
      q: "矿工反复做双重 SHA-256 的对象是？",
      options: ["整个区块的全部交易", "80 字节的区块头", "你的私钥", "mempool"],
      answer: 1,
      explain: "默克尔根已把交易压进头里，哈希头即承诺整块。",
    },
    {
      q: "区块头里的“默克尔根”作用是？",
      options: ["记录时间", "把本区块所有交易压成一个总指纹", "存储 nonce", "指向下一个区块"],
      answer: 1,
      explain: "改任何交易都会改变默克尔根，进而改变区块头。",
    },
    {
      q: "32 位的 nonce 全试完仍没达标，矿工会？",
      options: ["放弃", "改时间戳或默克尔根（extranonce）制造新的待试头继续", "降低难度", "等别人挖"],
      answer: 1,
      explain: "通过改动其它可变字段，继续产生新的 80 字节去试。",
    },
    {
      q: "“上一区块哈希”这个字段的意义是？",
      options: ["美观", "把本区块链接到前一个，形成不可篡改的链", "存交易", "记录矿工名字"],
      answer: 1,
      explain: "这正是区块链“链”的来源（阶段 2.1）。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Block header", url: "https://learnmeabitcoin.com/technical/block/block-header/" },
    { label: "learnmeabitcoin：Mining / nonce", url: "https://learnmeabitcoin.com/technical/mining/" },
  ],
};
