export default {
  id: "node-types",
  stage: 6,
  order: 2,
  title: "节点类型：全节点、剪枝节点、轻钱包",
  difficulty: "core",
  prereqs: ["p2p-network", "merkle-tree"],

  oneLiner:
    "不是所有节点都一样。全节点亲自验证一切、是去信任的基石；剪枝节点同样全量验证、但丢弃旧区块来省空间；轻钱包（SPV）只存区块头、用默克尔证明确认自己的交易，轻便但要多信任别人一点。",

  intuition: `
节点不是只有一种。可以按"自己存多少、自己验证多少、要信任别人多少"这三个维度，把它们分成几档——存得越多、验得越多，需要信任别人的就越少，但负担也越重。

最关键的一句话是 **"Don’t trust, verify（别信任，去验证）"**：你自己跑一个全节点，就不必相信任何人告诉你的链状态。轻钱包则在便携性和信任假设之间做了取舍。

右边点一点，对比三类节点各自存什么、信什么。

**这一节，我们把"节点类型"拆成四块：**

- **① 全节点——独立验证一切的去信任基石**
- **② 剪枝节点——同样全量验证，只是不囤旧区块**
- **③ 轻钱包（SPV）——只存区块头，用默克尔证明换便携**
- **④ 一个关键澄清——挖矿 ≠ 验证**
`,

  mechanics: `
### ① 全节点：独立验证一切的去信任基石

全节点下载并保存**整条链**（2026 年区块数据已超 **600 GB**，加索引更多），并**亲自验证每一条共识规则**：每笔交易的签名、每个 UTXO 的有无、每个区块的工作量证明、2100 万的发行上限……一项不合规就否决。

- **它谁都不信，只信自己算过的。** 别人发再多假消息——伪造的余额、凭空多出的币、双花的交易——你的节点照样按规则当场拒绝。
- **主权的真正含义。** 你不必再相信交易所、区块浏览器或任何人"转告"你链上发生了什么；规则的最终裁判权握在你自己手里。
- **共识的力量来自数量。** 正是因为全球**千千万万个全节点各自独立验证**，没有任何人能偷偷改规则——哪怕全体矿工合谋，节点也会集体否决不合规的块（回扣阶段 2.4；阶段 6.4 的分叉会再用到这一点）。

### ② 剪枝节点：同样全量验证，只是不囤旧区块

剪枝（pruning）常被误以为"安全性打折"，其实**完全不是**：

- 剪枝节点会**完整下载并校验每一个区块**——从创世块到链尖，一个不漏，验证强度和全节点**一模一样**。
- 区别只在于：校验完一个旧区块后，就**把它从硬盘删掉**，只保留最近一个可配置的窗口（最小约 **550 MB**）。当前的 UTXO 集合（chainstate）始终完整保留，所以它照样能独立验证新交易、新区块。
- **唯一的代价**：它没法再把完整的历史区块**提供给别人**下载（不能服务于新节点的初始同步），也不能响应需要老数据的查询。
- **适合谁**：硬盘吃紧的个人自托管者。用几百 MB 的存储，换来与全节点同等的去信任安全——性价比极高。

### ③ 轻钱包（SPV）：只存区块头，用默克尔证明换便携

SPV（Simplified Payment Verification，简易支付验证）是中本聪在白皮书第 8 节就提出的方案，专为手机这类资源受限设备设计：

- **只下载区块头。** 每个区块头才 **80 字节**，整条链的全部头加起来也只有**几十 MB**（对比全节点的几百 GB）。
- **怎么确认"我的交易入块了"？** SPV 钱包向全节点索取一份**默克尔证明**——一条从你的交易哈希通往区块头里那个默克尔根的短路径（回扣阶段 3.3）。证明能对上根，就说明这笔交易确实在那个区块里，而无需下载整块的几千笔交易。
- **它的信任假设。** SPV **假设"累积工作量最多的那条区块头链是诚实的"**，自己并不验证区块里的交易是否都合法（比如有没有人凭空印币）。所以它把"规则有没有被遵守"这件事，**外包给了全节点**——这就是它比自验弱的地方。
- **隐私代价。** 为了让全节点帮你筛选相关交易，SPV 往往要**透露你关心哪些地址**，隐私更差。
- **一句话取舍**：轻便、省电、适合手机；代价是更强的信任假设和更弱的隐私。要真正去信任，最好让手机钱包连上**你自己的全节点**。

### ④ 一个关键澄清：挖矿 ≠ 验证

这是初学者最常混的一点：**绝大多数节点不挖矿。**

- **矿工**负责"写新页"——把交易打包成块、做工作量证明（阶段 5）。全网真正在挖矿的实体其实**很少**（高度集中在几个矿池）。
- **全节点**负责"审核每一页是否合规"——不管块是谁挖的，只要不守规则，就一律拒收。运行全节点的人**成千上万**，遍布全球。
- 两者**分工、互相制衡**：矿工有算力但没有改规则的权力，因为节点会否决他们的违规块。这正是"**算力不等于话语权**"的根源（阶段 6.4 讲分叉时会看到，连软件升级都得由运行节点的经济多数说了算）。
`,

  demo: "node-types",

  analogy: `
- **全节点**像自己逐页核对账本**原件**的会计：谁也骗不了他。
- **剪枝节点**像核对完原件后，只留最近几页、把旧页碎掉的会计——每一页他都亲自查过，只是不再囤着。
- **轻钱包**像只拿到一份目录、外加一张"你的条目在第几页"的证明：方便，但得相信提供证明的人没耍花样。
`,

  misconceptions: [
    "“节点越多，是为了多存几份省空间。” —— 不是。每个全节点都各存一份完整账本，靠冗余与独立验证换取抗审查与去信任。",
    "“剪枝节点安全性打了折。” —— 没有。它对每个区块全量验证，只是验证后不再保存旧区块。",
    "“SPV 轻钱包和全节点一样安全。” —— 不一样。SPV 依赖更强的信任假设、隐私也更弱；自验的全节点最稳。",
    "“只有矿工才算节点。” —— 不。绝大多数节点不挖矿，只验证与转发；挖矿和验证是两回事。",
  ],

  quiz: [
    {
      q: "“Don’t trust, verify”最贴切地描述了哪种节点？",
      options: ["轻钱包（SPV）", "亲自验证一切的全节点", "交易所节点", "只看价格的行情软件"],
      answer: 1,
      explain: "全节点独立验证每条规则，不必信任任何人。",
    },
    {
      q: "剪枝节点和全节点的关键区别是？",
      options: ["验证强度更弱", "验证强度相同，只是丢弃旧区块来省存储", "不联网", "需要矿机"],
      answer: 1,
      explain: "全量验证不打折，只是不长期保存历史。",
    },
    {
      q: "轻钱包（SPV）靠什么确认自己的交易被打包？",
      options: ["下载整条链", "只存区块头，向全节点索取默克尔证明自行核对", "打电话问矿工", "完全相信交易所"],
      answer: 1,
      explain: "区块头 + 默克尔证明，轻便但信任假设更强（回扣 3.3）。",
    },
    {
      q: "关于“节点”和“矿工”，正确的是？",
      options: ["所有节点都在挖矿", "绝大多数节点不挖矿，只验证与转发；挖矿和验证是两回事", "只有矿工是节点", "节点负责发行新币"],
      answer: 1,
      explain: "矿工写新页，全节点审核每页是否合规，分工不同。",
    },
  ],

  further: [
    { label: "Bitcoin.org：为什么以及如何运行全节点", url: "https://bitcoin.org/zh_CN/full-node" },
    { label: "learnmeabitcoin：SPV / 轻钱包", url: "https://learnmeabitcoin.com/beginners/guide/" },
  ],
};
