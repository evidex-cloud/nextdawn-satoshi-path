export default {
  id: "address-types",
  stage: 4,
  order: 3,
  title: "地址类型：从 1... 到 bc1p...",
  difficulty: "core",
  prereqs: ["script"],

  oneLiner:
    "你见过的不同样子的比特币地址（1 开头、3 开头、bc1q、bc1p），其实对应着不同的“锁”。它们一步步演进，主要是为了更便宜、更安全、更隐私——新型的 SegWit 和 Taproot 地址手续费更低，也修掉了老地址的一些毛病。",

  intuition: `
比特币地址不是只有一种长相。常见的有四类，看**前缀**就能认出来。它们本质都是上一节那些"锁"的不同**编码**——同一条链、同样的币，只是装币的盒子换了样式：

- **\`1...\`（P2PKH）**：最古老的地址，2009 年创世起就有。兼容性最好，但交易体积偏大、手续费偏高。
- **\`3...\`（P2SH）**：把"一段脚本的哈希"藏在地址里，花费时再亮出完整脚本。多签、以及早期"包装版 SegWit"常用。
- **\`bc1q...\`（SegWit v0，P2WPKH）**：2017 年隔离见证带来的新地址，**更便宜**（见证折扣），还修复了"交易延展性"的老毛病。
- **\`bc1p...\`（Taproot，P2TR）**：2021 年的最新一代，基于 Schnorr 签名，**更隐私、更高效**——哪怕复杂的多签或合约，对外也能长得和普通转账一模一样。

右边点一点，认一认每种地址的前缀、年代、编码方式和特点。

**这一节，我们把"地址类型"拆成四块：**

- **① 地址其实是什么——锁的编码，不是账户**
- **② 两套编码体系——Base58Check 与 Bech32 的纠错之差**
- **③ 为什么一代代演进——延展性、见证折扣、隐私**
- **④ 实战取舍——该用哪种、收款要注意什么**
`,

  mechanics: `
### ① 地址其实是什么：锁的编码，不是账户

一个常见的误解是把地址当成"账户"。其实地址里**不存任何余额、也不存私钥**——它只是把某段锁定脚本（或它的哈希、或一个公钥）**编码成一串方便复制、不易抄错的字符**。

- 钱包给你一个 \`1...\` 地址，等价于说："请构造一个 P2PKH 锁定脚本，公钥哈希填这串解码出来的 20 字节。"付款方据此造好输出，钱就锁进了那把锁。
- 所以"我有 5 个地址"不代表"5 个账户"——每个地址对应一类锁；你的余额是钱包把所有这些锁住、属于你的 UTXO 加总出来的（阶段 4.1）。
- 一个地址被反复使用并无技术障碍，但每次复用都把更多 UTXO 绑在同一把锁上，**损害隐私**（阶段 1 的"每次用新地址"建议）。

### ② 两套编码体系：Base58Check 与 Bech32 的纠错之差

四类地址用了两套截然不同的编码，差别远不止"长得不一样"：

- **\`1...\` / \`3...\` 用 Base58Check**：取 58 个字符（刻意剔除了易混的 \`0\`、\`O\`、\`I\`、\`l\`），末尾附 4 字节校验和。它能**发现**抄错，但不能告诉你错在哪。
- **\`bc1q...\` / \`bc1p...\` 用 Bech32 / Bech32m**：全小写、字符集更受限，校验和基于 BCH 纠错码，不仅能**发现**错误，在少量字符出错时还能**定位**到大概是哪几位错了。\`bc1p\`（Taproot）改用 Bech32m，修掉了原始 Bech32 在某些长度下的一个编码弱点。
- 全小写还带来一个实际好处：地址可以**全大写**显示（如刻在硬件、印在纸上）而不歧义，二维码也更紧凑。

### ③ 为什么一代代演进：延展性、见证折扣、隐私

每一代地址，都在补上一代的洞：

- **交易延展性（malleability）**：早期交易里，第三方能在**不改变实质**的情况下"微调"签名数据，导致 \`txid\` 变化。这对依赖"未确认交易 \`txid\`"的闪电网络等二层是致命的。**SegWit** 把签名（见证）移到交易独立区域，\`txid\` 不再含签名，**从根上修掉了延展性**（阶段 4.5、8.2）。
- **见证折扣**：见证数据在计费时只按四分之一计权，于是 \`bc1q\` 交易的 vByte 更小、同费率下更便宜（阶段 4.4 细算）。这是"修延展性"顺带的红利，也是大家换 \`bc1\` 地址省钱的直接原因。
- **效率与隐私（Taproot）**：P2TR 用 **Schnorr 签名**（可把多个签名聚合成一个），并把一棵"花费条件脚本树"藏在单个公钥背后。只要各方配合走常规路径，链上看到的就是一笔最普通的单签转账——\`2-of-3\` 多签、时间锁合约对外**全部隐形**，既省空间又护隐私（阶段 8 深入）。

### ④ 实战取舍：该用哪种、收款要注意什么

- **默认用 \`bc1q\` 或 \`bc1p\`**：手续费更省、纠错更好。绝大多数 2020 年后的钱包都已支持。
- **收款先确认对方钱包认不认**：极老的钱包或交易所可能不识别 \`bc1p\`（Taproot），少数甚至不认 \`bc1q\`。收款失败往往不是地址错，而是**付款方软件太旧**。
- **\`3...\` 的双重身份**：它既可能是纯多签 P2SH，也可能是"包装版 SegWit"（P2SH-P2WPKH）——后者让老钱包能给"不认 \`bc1\`"的接收方付 SegWit 的钱，是过渡期的折中。
- **抄地址永远靠复制粘贴或扫码**：再好的纠错码也只防个别字符，防不了你把整段贴错。大额转账前，核对**首尾几位**已是社区惯例。
`,

  demo: "address-id",

  analogy: `
地址类型就像**快递面单的不同版本**：老版本（1...）哪个网点都收，但格子多、贴起来占地方（费用高）；新版本（bc1q、bc1p）更精简、更防填错，还附带更好的隐私与折扣——只是个别老网点可能还没升级、不认新单。

包裹（你的币）始终是同一个，变的只是"封装与寻址"的方式。
`,

  misconceptions: [
    "“不同前缀的地址是不同的币。” —— 不是。都是同一条链上的比特币，只是“锁”的类型和编码不同。",
    "“新地址（bc1）不安全或不正规。” —— 恰恰相反，它们更新、更省费、纠错更好、还修了老毛病；只是少数老旧钱包可能不支持。",
    "“反正都一样，地址可以一直重复用。” —— 类型不影响“最好每次用新地址”的隐私建议（阶段 1）；复用会把更多 UTXO 绑在同一把锁上。",
    "“SegWit 只是为了便宜。” —— 它首先解决了交易延展性，便宜（见证折扣）是顺带的红利，也为闪电网络铺了路。",
    "“地址里存着我的余额或私钥。” —— 都不存。地址只是某段锁定脚本的编码；余额是钱包对你 UTXO 的求和，私钥只在签名时本地使用。",
  ],

  quiz: [
    {
      q: "以下哪个是最新一代、基于 Schnorr 的地址类型？",
      options: ["1 开头（P2PKH）", "3 开头（P2SH）", "bc1q 开头（SegWit v0）", "bc1p 开头（Taproot）"],
      answer: 3,
      explain: "bc1p 即 Taproot（P2TR），2021 年引入。",
    },
    {
      q: "SegWit 最重要的贡献是？",
      options: ["让地址更短", "把签名移到独立区域，根治交易延展性，并带来费用折扣", "提高总量上限", "取消手续费"],
      answer: 1,
      explain: "延展性修复是关键，费用折扣是红利，并利好闪电网络。",
    },
    {
      q: "不同前缀的地址，本质区别在于？",
      options: ["背后是不同的币", "对应不同类型的“锁”（锁定脚本）与编码方式", "归不同公司管", "只是颜色不同"],
      answer: 1,
      explain: "地址是某段锁定脚本或公钥的编码，类型不同、币相同。",
    },
    {
      q: "用新型 bc1 地址收款，主要要注意什么？",
      options: ["会更不安全", "确认对方钱包支持该地址类型（极老钱包可能不认）", "必须先交押金", "只能小额"],
      answer: 1,
      explain: "功能更优，但需对方钱包兼容。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Script / 地址类型", url: "https://learnmeabitcoin.com/technical/script/" },
    { label: "维基百科：SegWit", url: "https://en.wikipedia.org/wiki/SegWit" },
  ],
};
