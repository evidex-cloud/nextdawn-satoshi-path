export default {
  id: "raw-transaction",
  stage: 4,
  order: 5,
  title: "交易的原始结构：逐字节读懂一笔交易",
  difficulty: "deep",
  prereqs: ["utxo", "script", "tx-size-fees"],

  oneLiner:
    "一笔比特币交易，本质就是一串有固定格式的字节。把它拆开，你会看到：版本号、输入列表（各指向一个被花的 UTXO）、输出列表（各创造一个新 UTXO）、SegWit 的见证区、以及 locktime。能像读句子一样读懂裸十六进制，你就从“比特币用户”跨进了“比特币原生”。",

  intuition: `
前面几节你已经懂了 UTXO、脚本、地址、费用。这一节把它们**落到一笔真实交易的字节上**——你将学会像读句子一样，把一串裸十六进制念出来。

每笔交易解码后，是这样一串**有固定顺序**的结构（从前往后）：版本号（4 字节）→ SegWit 的标记标志（仅隔离见证）→ 输入列表（每个指向一个被花的 UTXO）→ 输出列表（每个创造一个新 UTXO）→ 见证区（仅 SegWit）→ locktime（4 字节）。没有标签、没有分隔符，全靠**位置和长度**来界定每一格——这正是它能被全网一致解析的原因。

右边的**原始交易解码器**，把样例（或你从 mempool.space 复制的真实十六进制）解析成人话：版本、每个输入输出、脚本类型、locktime、vByte。

**这一节，我们把"裸交易"拆成四块：**

- **① 字段顺序与边界——从头到尾走一遍六大区段**
- **② 两个底层编码——小端序与 varint 这两个必踩的坑**
- **③ legacy 与 SegWit 的字节差异——marker、空 scriptSig、见证区**
- **④ sequence、locktime 与 txid——藏在末尾的三个机关**
`,

  mechanics: `
### ① 字段顺序与边界：走一遍六大区段

解码靠的是"读固定长度的格子，再用计数字段决定后面有几个变长格子"。逐段拆开：

- **版本号（4 字节）**：用哪套规则。常见 \`01000000\`（版本 1）或 \`02000000\`（版本 2，启用相对时间锁）。注意它已经是小端序（见 ②）。
- **标记 + 标志（2 字节，仅 SegWit）**：\`0001\`。版本号后若紧跟这两字节，就宣告"这是隔离见证交易"。
- **输入数量**（varint）→ 每个**输入**依次是：被花 UTXO 的 \`前一笔 TXID\`（32 字节）+ \`输出序号 vout\`（4 字节）+ \`scriptSig 长度\`（varint）+ \`scriptSig\`（解锁脚本）+ \`sequence\`（4 字节）。
- **输出数量**（varint）→ 每个**输出**依次是：\`金额\`（8 字节，单位聪）+ \`scriptPubKey 长度\`（varint）+ \`scriptPubKey\`（锁定脚本）。
- **见证区**（仅 SegWit）：每个输入对应一组见证项（被从 scriptSig 挪出来的签名、公钥等），各自带 varint 计数和长度。
- **locktime（4 字节）**：最早能上链的时间或高度。

走一个迷你例子：\`scriptPubKey\` 是 \`OP_DUP OP_HASH160 <20字节> OP_EQUALVERIFY OP_CHECKSIG\` 的 P2PKH 输出，其字节是 \`76 a9 14 <20字节公钥哈希> 88 ac\`——\`14\` 是十六进制的 20，告诉解码器"接下来 20 字节是公钥哈希"。能认出 \`76a914…88ac\` 这个固定外形，你就能一眼判定"这是个 P2PKH 输出"。

### ② 两个底层编码：小端序与 varint

这是写解码器时最常踩的两个坑：

- **小端序（little-endian）**：多字节**数值**是**倒着存**的（低位字节在前）。例：1 BTC = 100,000,000 聪 = 十六进制 \`05F5E100\`，在交易的金额字段里写成 \`00E1F50500000000\`（8 字节、低位在前）。读的时候要先把字节序反转再当数字。**前一笔 TXID 在裸数据里也是反着的**——所以区块浏览器显示的 txid 和裸交易里那 32 字节，字节顺序恰好相反，这是新手最容易困惑的一点。
- **varint（变长整数）**：计数与长度字段（输入数、输出数、脚本长度）用 1～9 字节变长编码省空间：值 < \`0xFD\`(253) 就用 **1 字节**直接写；否则用前缀标记后续长度——\`fd\` + 2 字节、\`fe\` + 4 字节、\`ff\` + 8 字节。所以看到一个 \`fd\`，别当它是数据，它是在说"真正的数量在后面 2 字节里"。

### ③ legacy 与 SegWit 的字节差异

同一笔转账，legacy 编码和 SegWit 编码的字节布局不同，认清差异是读真实交易的关键：

- **怎么一眼认 SegWit？** 版本号之后**紧跟 \`0001\`**（marker \`00\` + flag \`01\`）。legacy 交易这里直接就是输入数量的 varint（绝不会是 \`00\`，因为没有零输入的交易）。
- **SegWit 下各输入的 scriptSig 为空**：解锁数据（签名、公钥）全搬到了末尾的见证区，对应输入的 \`scriptSig 长度\`是 \`00\`。legacy 交易则把签名公钥塞在 scriptSig 里、没有见证区。
- **这正是费用折扣的来处**：见证区的字节按四分之一计权（阶段 4.4）。把签名挪进见证区，既修了延展性，又省了钱（阶段 8.2）。

### ④ sequence、locktime 与 txid：末尾的三个机关

- **sequence（每输入 4 字节）**：早期为"交易替换"设计的字段，如今身兼两职——值 ≤ \`0xFFFFFFFD\` 时启用 **RBF（费率替换，回扣阶段 1.3）**，允许你之后用更高费率覆盖这笔；它还参与 **相对时间锁（BIP68）**。设成 \`ffffffff\` 表示"无相对锁、最终版、不可 RBF"。
- **locktime（4 字节）的两种含义**：数值 < **500,000,000** 解释为**区块高度**，≥ 则为 **Unix 时间戳**。\`0\` 表示可立即上链。这让你能造"到第 85 万块前任何人都打包不了"的交易——比如时间锁继承、托管退款。
- **txid 怎么来**：把**整笔交易做双重 SHA-256**，再把结果字节反转显示。关键点：**SegWit 交易算 txid 时不包含见证区**——签名不进 txid，第三方就无法靠改签名来改 txid，这就是"延展性被根治"的字节级原因（阶段 8.2）。SegWit 另有一个含见证的 \`wtxid\`，用于网络层和见证默克尔根。
`,

  demo: "tx-decoder",

  analogy: `
裸交易就像一封**按固定格式排版的电报**：第几格是日期、第几格是收件人、第几格是金额，位置和长度都有规矩。

外行看是一串乱码；可一旦你知道"第 1 格 4 字节是版本、接下来是输入清单……"，就能像读句子一样把它念出来——"版本 2，一个输入花 TXID:0，两个输出共 0.01002 BTC，无时间锁"。
`,

  misconceptions: [
    "“交易里的数字是正常顺序写的。” —— 不是。多字节数值用小端序（倒序）存储，TXID 在裸数据里也是反的（与浏览器显示恰好相反）。",
    "“数量字段都是固定 1 字节。” —— 用 varint 变长编码：值 < 253 用 1 字节，更大用 fd/fe/ff 前缀加 2/4/8 字节。",
    "“SegWit 交易也把签名放在 scriptSig 里。” —— 不。SegWit 下 scriptSig 为空（长度 00），签名移到见证区，txid 不再含签名。",
    "“locktime 一定是时间。” —— 小于 5 亿（500000000）当作区块高度，大于等于才当作 Unix 时间戳。",
    "“裸交易里有字段名或分隔符帮你区分。” —— 没有。全靠固定长度 + varint 计数来界定每一格，解码完全依赖位置顺序。",
  ],

  quiz: [
    {
      q: "怎么从裸十六进制一眼认出这是 SegWit 交易？",
      options: ["看金额", "版本号后紧跟 0001（marker+flag）", "看地址", "看 locktime"],
      answer: 1,
      explain: "0001 标记之后，scriptSig 为空、签名在见证区。",
    },
    {
      q: "交易里 1 BTC 的金额字段是怎么存的？",
      options: ["十进制 1", "100000000 聪，按小端序的 8 字节", "字符串 “1 BTC”", "随机"],
      answer: 1,
      explain: "单位是聪、8 字节、小端序（倒着存）。",
    },
    {
      q: "varint 是用来编码什么的？",
      options: ["金额", "数量/长度这类字段，按大小变长（1–9 字节）", "签名", "地址"],
      answer: 1,
      explain: "输入数、输出数、脚本长度都用 varint。",
    },
    {
      q: "locktime 字段值是 850000，含义是？",
      options: ["Unix 时间戳", "区块高度锁：到第 850000 块前不可上链", "手续费", "版本号"],
      answer: 1,
      explain: "小于 5 亿当作区块高度，大于才当作时间戳。",
    },
  ],

  further: [
    { label: "learnmeabitcoin：Transaction（逐字段可视化）", url: "https://learnmeabitcoin.com/technical/transaction/" },
    { label: "Mastering Bitcoin · 第 6 章 Transactions", url: "https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch06_transactions.adoc" },
  ],
};
